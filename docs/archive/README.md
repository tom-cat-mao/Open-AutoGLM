# docs/archive — 历史执行文档（冻结）

本目录存放**已执行完毕**的批次任务书（`execution-*.md`）与已被实现取代的设计稿/路线草案。
它们是历史记录，内容按写作当时的仓库状态冻结，**不随代码维护**：

- 文中引用的 `.trae/rules/*` 路径已随 TraeCLI 退役被删除；
- 文中的拓扑、state 字段、阶段状态可能与当前代码不符。

当前真相源：仓库根 `AGENTS.md`（P0 约束）、`CLAUDE.md`、`README.md`、
`docs/future-roadmap.md`，以及 `phone_agent/graph/builder.py` docstring（图拓扑）。
