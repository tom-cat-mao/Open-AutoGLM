Pillow>=12.0.0
openai>=2.9.0

# For LangGraph Agent Architecture
langgraph>=0.2.0
langchain-core>=0.3.0

# For iOS Support
requests>=2.31.0

# For Model Deployment

## After installing sglang or vLLM, please run pip install -U transformers again to upgrade to 5.0.0rc0.
## Any dependency conflicts related to Transformers can be ignored.

# sglang>=0.5.6.post1
# vllm>=0.12.0
# transformers>=5.0.0rc0

# Optional Visual Grounding (LocateAnything on Apple Silicon MLX)
# Install with: pip install -e ".[locateanything]"
# mlx-vlm is kept out of default requirements so CI and non-MLX hosts do not need it.

# Development & Testing
pytest>=7.0.0
black>=23.0.0
mypy>=1.0.0
ruff>=0.1.0
