---
description: RALPLAN 使用 OMX consensus planning；旧 Trae-native graph.mdc 规划状态机已移除
alwaysApply: false
globs: ".codex/skills/ralplan/SKILL.md,.codex/skills/plan/SKILL.md,.codex/skills/deep-interview/SKILL.md,.trae/traecli.toml,.trae/rules/graph.mdc"
---

# RALPLAN Boundary

Open-AutoGLM 不再维护 Trae-native RALPLAN 状态机。

## Canonical workflow

RALPLAN 必须使用 oh-my-codex 最新 consensus planning：

```text
$ralplan == $plan --consensus
Planner -> Architect -> Critic
```

详细协议以 `.codex/skills/ralplan/SKILL.md` 与 `.codex/skills/plan/SKILL.md` 为准。

## Removed legacy surfaces

以下旧入口已删除，不得恢复：

- `.trae/skills/ralplan/`
- `.trae/commands/ralplan.md`
- `.trae/hooks/ralplan.py`
- `.trae/ralplan/state.json` / `.trae/ralplan/stop-breaker.json` 作为 workflow runtime
- `graph.mdc` 内的旧 RALPLAN approval ledger 作为唯一计划产物

## Artifact boundary

- RALPLAN 计划、PRD、test-spec、consensus evidence 使用 OMX artifacts：`.omx/context/**`、`.omx/plans/**`、`.omx/specs/**`、`.omx/state/**`。
- Architect/Critic durable evidence 必须按 OMX 协议记录，不能仅靠 `graph.mdc` 中的摘要字段证明 consensus complete。
- `.trae/rules/graph.mdc` 只保留 Open-AutoGLM 架构 roadmap、项目不变量和执行约束镜像。

## Execution boundary

Critic approval 前不得进入实现；approval 后默认 handoff 到 `$ultragoal`，只有用户明确选择时才使用 `$team` 或 `$ralph` fallback。
