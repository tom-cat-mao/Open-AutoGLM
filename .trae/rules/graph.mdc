---
description: LangGraph roadmap 与核心执行约束；仅在 RALPLAN/Autopilot 规划阶段加载
alwaysApply: false
globs: ".trae/commands/ralplan.md,.trae/commands/autopilot.md,.trae/hooks/ralplan.py,.trae/hooks/autopilot.py,.trae/rules/ralplan.mdc,.trae/rules/autopilot.mdc,.trae/rules/graph.mdc"
---

# RALPLAN Status

```yaml
status: completed
design_status: critic_approved
execution_status: completed
iteration: 1
planner_mode: null
revision_scope: null
last_planner_update: "Targeted revision after Architect ITERATE: protected reflection parser migration, specified deterministic mark hash fallback, added layered error schemas, and preserved LangGraph reducer invariants."
last_architect_review: "APPROVE_FOR_CRITIC: prior blocker/major findings resolved; minor provider file-scope note is execution-time only."
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "User handed off to Autopilot for execution. Six-phase implementation and regression tests completed; old long historical content has been replaced with a concise self-contained roadmap."
```

# Roadmap

## 已完成 Phase

- ✅ 历史 Phase：已完成基础 LangGraph 主循环、设备抽象、HITL、trace/eval、context harness、IntentIR/ActionIR、grounding provider 与安全执行管线；本文件仅保留当前规划需要的摘要，不保留旧长篇历史记录。

## RALPLAN-DR 摘要

### Principles

1. **结构化执行唯一入口**：执行路径以 JSON schema / tool calls / IntentIR 为准；旧 text DSL 不再作为动作执行协议。
2. **删除执行 DSL，不误删安全解析**：删除 action execution text DSL；reflection/parser 路径迁移为结构化 verdict/failure schema；如保留 `parse_action()`，仅作为内部非执行 AST/literal 安全辅助。
3. **Grounding fail-closed**：mark stale、hash 低置信、多 bbox 歧义、provider 缺失、低置信候选都不能静默回退为主 VLM 坐标或 first-box 点击。
4. **错误分层可观测**：parse、adapter、validation、grounding、safety、execution、reflection、context 分层记录，避免 grounding failure 被包成 `model_parse_failed`。
5. **请求侧有界记忆**：保持 LangGraph state/reducer 审计语义；只压缩 request-time `request_messages`，用短期记忆替代完整旧文本历史。

### Decision Drivers

1. `phone_agent/config/__init__.py` 当前暴露 `LEGACY_PROMPT_VERSION`、`VALID_OUTPUT_MODES={"text_dsl","json_schema","tool_calls","auto"}` 与 `get_system_prompt(... output_mode="text_dsl")`，导致旧执行协议仍可被选中。
2. `phone_agent/graph/marks.py` 当前以 `current_app|widthxheight|screenshot_b64` 做 hash，截图微扰过敏；但简单放宽会增加 stale mark 误点风险。
3. `phone_agent/grounding/parser.py` 对多个 valid boxes 取第一个、`plan.py` 将 grounding/parse 混用、`state.py`/`agent.py` 未完整传播分层错误，导致恢复策略和 eval 归因不可靠。

### Viable Options

| Option | Pros | Cons |
|---|---|---|
| A. 继续保留 text DSL 执行兼容，仅补测试 | 短期改动少；旧用例容易保留 | 执行双轨持续存在；`plan.py` 默认 text_dsl 与 `reflect.py` text_dsl parser 继续混淆结构化管线；错误分层难以收敛 |
| B. 删除 action execution text DSL，迁移 plan/reflect 到结构化 schema，同时补强 mark binding、multi-box、failure taxonomy 与 context window | 架构边界清晰；可 fail-closed；trace/eval 可观测；长期维护成本低 | 需要同步配置、prompt、model client、adapter/handler、state/agent、graph nodes 与测试 |
| C. 只修 mark hash 与 multi-box，不动 legacy/context | 风险较局部 | 根因未解决：legacy 执行入口、reflection text parser、parse retry 与 grounding failure 混杂、messages 膨胀仍存在 |

### Selected Approach

选择 **B. 删除执行 text DSL + 结构化 reflection + screen-bound grounding hardening + 分层错误 + bounded context**。范围明确区分：删除的是 **action execution text DSL**；`reflect_node` 当前的 `output_mode="text_dsl"` 和自有 parser 必须迁移到 JSON verdict/failure schema；`parse_action()` 若保留，只能是内部非执行 safe parser/helper，不得作为 plan→execute 的模型动作入口。

### ADR

- **Decision**: 六阶段完成 legacy 执行 DSL 删除、反思结构化输出、MarkRegistry/screen binding 鲁棒化、multi-box fail-closed、全链路错误分层、context window 与回归清理。
- **Drivers**: 消除执行双轨；降低 mark hash 过敏但不牺牲 stale 防线；把 grounding ambiguity/provider/hash failure 与 parse failure 分离；维持 LangGraph reducer 不变量并控制 token 增长。
- **Alternatives**: 保留 legacy 回滚；仅放宽 raw hash；多个 bbox 继续取第一个；直接裁剪 `state["messages"]`。
- **Why chosen**: 当前安全边界依赖结构化 ActionIR、validator、safety gate 和 grounding fail-closed；只有同时收敛执行入口、错误协议和 request compaction 才能可测、可审计。
- **Consequences**: 已删除/迁移旧 prompt/config/test，新增 schema 字段与 RunResult 传播，更新 trace/eval/context contract；执行后通过回归测试与只读审查修复。
- **Follow-ups**: 后续如需更新 README/长期路线图应单独执行；本次按用户要求只提交核心代码与规则/测试变更，不创建额外文档。

## 当前规划 Phase

### Phase 1 — 删除 legacy text DSL 执行路径，并迁移 reflection 结构化输出

- **目标/根因**: 删除旧 action execution text DSL，解决 `config`/`plan_node` 默认 text_dsl 与结构化 IntentIR 管线并存的问题；同时保护 `reflect_node`，不要把 reflection 的安全解析能力误删，而是迁移为结构化 verdict/failure schema。
- **修改文件候选**: `phone_agent/config/__init__.py`, `phone_agent/config/prompts_zh.py`, `phone_agent/config/prompts_en.py`, `phone_agent/model/client.py`, `phone_agent/actions/handler.py`, `phone_agent/actions/__init__.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/nodes/reflect.py`, `tests/actions/test_parse_action.py`, `tests/actions/*`, `tests/model/*`, `tests/graph/test_plan_reflect.py`。
- **执行步骤**:
  1. 删除/迁移 `LEGACY_PROMPT_VERSION`、legacy prompt 常量、`VALID_OUTPUT_MODES` 中的 `text_dsl` 执行模式，以及 `get_system_prompt(... output_mode="text_dsl")` 默认路径；`auto` 必须解析到结构化 JSON/tool_calls/IntentIR。
  2. 将 `phone_agent/graph/nodes/plan.py` 默认 `output_mode` 从 `text_dsl` 改为结构化模式，并移除 plan→`parse_action(response.action)` 作为模型动作执行入口。
  3. 将 `phone_agent/graph/nodes/reflect.py` 从 `output_mode="text_dsl"` 与自有文本 parser 迁移到结构化 JSON verdict/failure schema，输出稳定字段如 `reflection_verdict`、`failure_cause`、`suggested_strategy`、`error_layer`、`error_code`。
  4. `parse_action()` 若仍保留，必须明确为内部非执行 helper：仅可用于安全 AST/literal 解析、旧 fixture 迁移或 reflection 兼容辅助；不得从主执行入口导出为可执行协议，`phone_agent/actions/__init__.py` 的 export 需同步收敛。
  5. 删除或迁移 legacy text DSL tests，新增结构化输出唯一入口、legacy DSL 被拒绝、reflection JSON schema 解析成功/失败的回归测试。
- **验收标准**:
  - 代码搜索确认无可配置 action execution `text_dsl` output mode、无 `legacy_text_dsl` 回滚路径、无 `plan.py` 默认 text_dsl 或 plan→execute `parse_action(response.action)` 执行路径。
  - `reflect_node` 仍能安全处理模型反思输出，但输出为结构化 verdict/failure，不依赖旧 text DSL 作为动作执行协议。
  - `parse_action()` 如存在，只在内部非执行上下文使用，并继续满足 `ast.parse` + `ast.literal_eval` 安全约束；旧 DSL 不能产生设备动作。

### Phase 2 — MarkRegistry / screen binding hardening

- **目标/根因**: 修复 mark id screenshot accuracy 判定过敏问题。当前 `phone_agent/graph/marks.py` 用 `current_app|widthxheight|screenshot_b64` 计算 hash，任何像素级扰动都可能 stale；但 mark 绑定仍必须防止跨屏误点。
- **修改文件候选**: `phone_agent/graph/marks.py`, `phone_agent/graph/observation.py`, `phone_agent/actions/grounding.py`, `phone_agent/graph/state.py`, `tests/graph/*`, `tests/actions/*`。
- **执行步骤**:
  1. 为 observation/mark set 引入 `semantic_screen_id`、`observation_epoch`、`mark_set_version`、屏幕尺寸、当前 app、mark topology 摘要；mark 使用前必须校验同 epoch/version 或通过语义/感知匹配。
  2. 本地实现确定性 normalized/perceptual hash：优先使用 Pillow（若可用）将截图 resize 到固定小尺寸、grayscale、可选 threshold，并计算 aHash/dHash/normalized hash 与 Hamming distance；阈值集中放在 config/constants，测试固定种子/fixture。
  3. Pillow 不可用时使用 stdlib/现有图像字节能力的 deterministic fallback：尺寸、current_app、layout features、mark topology、元素中心/面积分布、可访问文本摘要 hash；raw screenshot hash 仅 audit-only，不作为唯一 stale 判定。
  4. 设计 deterministic fallback 策略：高置信 semantic + topology + size 稳定可通过；低置信、关键 layout feature 改变、threshold 超限或 version 不兼容必须 fail-closed/reobserve。
  5. grounding 使用 mark 前统一调用 binding validator，返回结构化 binding result 和 failure code。
- **验收标准**:
  - 单测覆盖 raw hash 改变但 normalized/perceptual hash 和 topology 稳定时通过、关键布局/mark topology 变化时 stale、Pillow 不可用 fallback、阈值边界、低置信 fail-closed。
  - trace/eval 只记录 `semantic_screen_id`、`observation_epoch`、`mark_set_version`、hash 摘要、distance/threshold、binding failure code，不记录原始截图。
  - stale/low-confidence mark 不进入 executor，不回退主 VLM 坐标；可恢复时触发 reobserve/wait/takeover 策略。

### Phase 3 — Multi-box grounding ambiguity fail-closed

- **目标/根因**: 修复 `phone_agent/grounding/parser.py` 多个 valid boxes 时选择第一个的问题；多个有效 bbox 表示目标歧义，静默 first-box 会造成误点。
- **修改文件候选**: `phone_agent/grounding/parser.py`, `phone_agent/actions/grounding.py`, `phone_agent/actions/adapter.py`, `phone_agent/actions/validator.py`, `phone_agent/graph/state.py`, `tests/actions/test_grounding_provider.py`, 相关 parser/graph/eval 测试。
- **执行步骤**:
  1. 将 provider/parser contract 改为返回候选集 `candidates[]`：每个 candidate 包含 bbox、confidence、source、normalized bbox、validity reason；顶层包含 `candidate_count` 与 `grounding_status`。
  2. 明确成功条件：**exactly one valid candidate** 才能进入 canonical ActionIR；zero candidate 产生 `grounding_no_candidate` / `unknown_mark`；multiple valid candidates 产生 `grounding_ambiguous` fail-closed。
  3. 禁止 executor 或 adapter 对多个候选静默取第一个；可选 disambiguation 只能通过结构化候选摘要交给 reobserve/reflection/下一轮 plan。
  4. trace/eval/context 增加 `candidate_count`、`selected_candidate_id`、`ambiguity_reason`、`grounding_failure_code`，并脱敏 target 文本。
- **验收标准**:
  - 单测覆盖 0/1/多个有效 bbox、多个同置信候选、低置信候选、越界/面积异常 bbox；只有 exactly one valid bbox 能执行。
  - 多候选歧义必须进入 `error_layer=grounding`、`error_code=grounding_ambiguous`，且设备动作未执行。
  - 代码搜索确认无“多个 valid boxes 取第一个”的残留逻辑。

### Phase 4 — Grounding failure 与全链路错误分层

- **目标/根因**: 将 grounding/provider/mark/bbox failure 从模型 parse failure 中拆出。当前 `plan.py` 中 grounding 错误常被包装为 model parse failure，导致 parse retry、reflection 提示、trace/eval 归因错误。
- **修改文件候选**: `phone_agent/graph/state.py`, `phone_agent/agent.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/nodes/reflect.py`, `phone_agent/actions/grounding.py`, `phone_agent/actions/adapter.py`, `phone_agent/actions/validator.py`, `phone_agent/graph/trace.py`, `phone_agent/graph/context.py`, `tests/graph/test_agent_run_result.py`, `tests/evals/test_eval_harness.py`, `tests/graph/test_trace.py`, `tests/graph/test_plan_reflect.py`。
- **执行步骤**:
  1. 在 `phone_agent/graph/state.py` 增加/收敛 schema 字段：`error_layer`、`error_code`、`recoverable`、`retry_policy`、`failure_cause`、`grounding_failure_code`、候选摘要字段，并保持 custom messages reducer 不变量。
  2. 在 `phone_agent/agent.py` / RunResult / public run result 中传播分层错误字段，保证调用方、eval harness 和 trace 能看到同一 error contract。
  3. 定义稳定 taxonomy：`parse`、`adapter`、`validation`、`grounding`、`safety`、`execution`、`reflection`、`context`；`provider_unavailable`、`unknown_mark`、`hash_mismatch`、`low_confidence`、`grounding_ambiguous` 属于 grounding 层。
  4. parse retry 仅处理 JSON/tool_calls/IntentIR 格式或 adapter parse 错误；grounding failure 不触发 parse retry，旧 `model_parse_failed` 不再用于 grounding failure。
  5. `plan.py`/`reflect.py` 消费分层错误：reflection 不得把 grounding ambiguity/hash mismatch 提示成“模型格式错误”，context/failure memory 记录结构化失败。
- **验收标准**:
  - 单测覆盖 malformed JSON/tool_calls 触发 parse/adapter retry；provider unavailable、unknown mark、hash mismatch、low confidence、ambiguous 不触发 parse retry。
  - `state.py`、`agent.py` RunResult、trace JSONL、eval JSON 均包含一致的 `error_layer/error_code/recoverable/retry_policy` 字段。
  - 代码搜索确认 grounding failure 不再使用旧 `model_parse_failed`，且所有 grounding failure 均 fail-closed 或进入显式 reobserve/wait/takeover 策略。

### Phase 5 — Context window 与 short-term memory 替代完整文本历史

- **目标/根因**: 历史图片已剥离，但完整旧文本 messages 仍可能线性膨胀；需要用短期记忆和 request compaction 控制上下文，同时严格保护 LangGraph reducer 语义。
- **修改文件候选**: `phone_agent/graph/context.py`, `phone_agent/graph/state.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/nodes/reflect.py`, `tests/graph/test_plan_reflect.py`, `tests/graph/test_trace.py`, `tests/evals/*`。
- **执行步骤**:
  1. 明确 reducer invariant：`compact_messages_for_request()` 只操作传给 `model_client.request()` 的 request copy / `request_messages`，**绝不 mutate `state["messages"]`**。
  2. `plan_node` 只返回新增 messages，不能返回裁剪后的完整 state；`execute_node` 现有 replacement semantics 保持不变，避免破坏 custom messages reducer。
  3. 在 `state.py` 增加 context memory 字段：`short_term_memory`、`action_ledger`、`screen_belief`、`failure_memory`、`grounding_observation`；字段默认脱敏、有大小/条数上限。
  4. request compaction 保留最新截图、最近 K 步必要摘要和 bounded memory sections；旧自由文本历史转结构化摘要，不把 reflection 长文本无限回注入 prompt。
  5. context 注入不得修改 ActionIR、HITL、pending_execute、interrupt、safety route 字段，不得绕过 confirm/takeover。
- **验收标准**:
  - 单测证明 `state["messages"]` 未被 `compact_messages_for_request()` 原地修改；`request_messages` 有界且保留最新截图。
  - `plan_node` reducer 行为仍为 append new messages；`execute_node` replacement 行为未回退；长任务 request token/消息数量不线性增长。
  - memory sections 能为下一轮 plan 提供短期上下文，但隐私文本已脱敏，且不会改变 ActionIR/HITL/safety 决策字段。

### Phase 6 — 回归测试、contract 固化与删除清理

- **目标/根因**: 将前五阶段的架构边界固化为测试和代码搜索约束，防止 legacy DSL、first-box 选择、错误混层、context mutation 回归。
- **修改文件候选**: `tests/model/*`, `tests/actions/*`, `tests/graph/*`, `tests/evals/*`, 必要 fixture；清理废弃 prompt/config/test fixture，但不得新增无关文档。
- **执行步骤**:
  1. 删除/迁移 legacy text DSL tests，补充 JSON schema/tool_calls/IntentIR 唯一执行入口、reflection structured schema 的单元和集成测试。
  2. 为 mark binding normalized/perceptual hash、Pillow unavailable fallback、multi-box ambiguity、failure taxonomy、RunResult propagation、context compaction reducer invariant 增加回归测试。
  3. 更新 trace/eval golden schema，断言新增 error/candidate/context memory 字段；测试默认不依赖真实模型、真实设备或外部服务。
  4. 做删除清理与代码搜索：无 `legacy_text_dsl` 执行入口、无 `text_dsl` plan/reflect 输出模式、无 first valid box 静默选择、无 grounding failure 混入 `model_parse_failed`。
- **验收标准**:
  - 建议测试命令 `.venv/bin/pytest tests/model tests/actions tests/graph tests/evals -q` 通过，或失败项均为环境/外部依赖且有明确说明。
  - 代码搜索与测试共同证明：legacy 执行 DSL 已删除，reflection 已结构化，mark binding 阈值/回退确定，multi-box fail-closed，错误分层贯穿 state/agent/trace/eval，context reducer invariant 未破坏。
  - 所有新增/迁移测试不记录原始截图、API key 或隐私文本。

# 约束 Checklist

- [x] 当前状态为 `design_status: critic_approved`、`execution_status: completed`、`approved_for_execution: true`；用户已明确要求 Autopilot 执行、测试并提交核心代码。
- [x] Planner 轮已结束；执行阶段允许修改批准范围内业务代码、测试与 `.trae/rules/graph.mdc`，不创建额外文档。
- [x] BLOCKER-1 已实现：删除范围区分 action execution text DSL 与 reflection/parser 安全解析；`reflect_node` 已迁移为结构化 JSON verdict/failure schema；`parse_action()` 仅保留为内部非执行 safe parser。
- [x] Phase 1 文件范围已覆盖 `phone_agent/config/__init__.py`、`phone_agent/config/prompts_zh.py`、`phone_agent/config/prompts_en.py`、`phone_agent/model/client.py`、`phone_agent/actions/__init__.py`、`phone_agent/graph/nodes/plan.py`、`phone_agent/graph/nodes/reflect.py` 与相关测试。
- [x] MAJOR-1 已实现：mark/screen binding 使用 Pillow aHash 与 deterministic fallback（semantic/layout/mark topology）；raw hash audit-only；低置信 fail-closed。
- [x] MAJOR-2 已实现：错误分层更新 `phone_agent/graph/state.py` schema 与 `phone_agent/agent.py` RunResult/public result，并覆盖 plan/execute/reflect、trace/eval/context；grounding failure 不再使用旧 `model_parse_failed`。
- [x] MAJOR-3 已实现：context window 不 mutate `state["messages"]`；`compact_messages_for_request()` 只处理 request copy；`plan_node` append new messages；`execute_node` replacement semantics 保持不变。
- [x] Multi-box grounding 遵守 exactly one valid candidate succeeds；zero/multiple valid candidates fail-closed；禁止 first-box 静默选择。
- [x] parse retry 只服务 parse/adapter 格式错误；grounding/provider/mark/hash/ambiguity/low-confidence/validation failure 不触发 parse retry，不回退主 VLM 坐标。
- [x] 坐标仍统一为 0-1000 相对坐标；tool/backend 是唯一 relative-to-absolute 转换点；不得 double conversion。
- [x] HITL 不变量保持：支付/隐私 confirm，登录/验证码 takeover；context、summary、grounding、reflection 不得绕过 safety gate、pending_execute 或 interrupt。
- [x] trace/eval/context 默认脱敏：不得记录原始截图、API key、隐私文本；只记录 hash 摘要、候选摘要、error code 与 bounded memory。
- [x] 回归测试覆盖 legacy 删除、reflection structured schema、mark hash 微扰/过期/fallback、multi-box ambiguity、failure taxonomy、RunResult propagation、context compaction reducer invariant。
