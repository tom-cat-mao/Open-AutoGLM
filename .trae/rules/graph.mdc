---
description: LangGraph roadmap 与核心执行约束；仅在修改 graph/Agent loop/Tool/HITL 或执行 roadmap 时加载
alwaysApply: false
globs: "phone_agent/graph/**/*.py,phone_agent/agent.py,phone_agent/actions/**/*.py,phone_agent/device_factory.py,phone_agent/adb/**/*.py,tests/**/*.py,setup.py,README.md,docs/**/*.md"
---

# RALPLAN Status

```yaml
status: completed
design_status: critic_approved
execution_status: completed
iteration: 1
planner_mode: null
revision_scope: null
last_planner_update: "按 Planner feedback_revision 协议对 Phase 10 做语义窄修：明确新增项目级 Autopilot stage agents、复用 RALPLAN 三件套、补齐 Stage→Agent→路径→权限/读写→职责映射与测试验收；不进入实现。"
last_architect_review: "复审无阻塞：已明确新增 6 个 stage agents、复用 RALPLAN 三件套、Stage→Agent 映射、测试验收、traecli.yaml 不改边界与 QA 主 Agent 最终责任。"
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "Phase 10 已完成：新增项目级 Autopilot stage agents，更新 hook/rule/skill/command，并补充 agent contract 与 prompt 一致性测试。"
```

# Roadmap

## 已完成 Phase

| Phase | 结论 | 不变量迁移位置 |
|---|---|---|
| ✅ 1 | Plan-Execute-Reflect MVP 已迁移到 LangGraph StateGraph | 见下方「约束 Checklist」State/Graph loop/context 条目 |
| ✅ 2 | HITL 已使用 `interrupt()` 接入 confirm/takeover；当前只保证路由语义，不承诺持久 resume | 见 HITL、pending_execute、checkpoint 条目 |
| ✅ 3 | Action 已封装为 graph tools，`dispatch_tool()` 统一调度，坐标转换集中在 tool 层 | 见 Tool DI、坐标转换条目 |
| ✅ 4 | 旧 while loop / ActionHandler 类 / use_graph / use_tools 双轨已清理 | 见禁止恢复旧双轨条目 |
| ✅ 5 | 已修复 messages reducer、confirm-then-execute、reflect i18n、mypy clean 等问题 | 见 messages reducer、confirm-then-execute 条目 |
| ✅ 6 | 已恢复测试/打包/文档可信地基；Tool DI seam 已通过 config 注入且 schema 隐藏 `device_factory` | 见 Phase 6 门禁、依赖注入、Tool DI 条目 |
| ✅ 7 | 已新增 `RunResult` / `run_structured()` 结构化 API 与本地 dry-run Evaluation Harness；Phase 7 仅统计 HITL interrupt routing，不承诺持久 resume | 见 Eval / Trace、HITL、checkpoint 条目 |
| ✅ 8 | 已新增默认本地 JSONL trace；记录 run/step/node/event/timestamp，覆盖 plan/execute/reflect/HITL/end/error；默认脱敏敏感文本与图片/API key；RunResult 与 eval JSON 可关联 trace 文件 | 见 Eval / Trace、隐私与收益验证条目 |
| ✅ 9 | 已新增结构化 reflection schema、失败原因分类、建议策略、Plan 反馈闭环，以及 eval retry/failure_cause 统计 | 见 Reflection、Eval / Trace 条目 |
| ✅ 10 | 已完成 TraeCLI-native Autopilot 多 subagent 编排移植：新增 6 个 stage agents，复用 RALPLAN 三件套，hook/rule/skill/command/tests 已对齐 | 见 Autopilot TraeCLI-native、subagent 编排、agent contract 测试条目 |

已完成 Phase 不再展开实现细节；执行约束统一维护在文末 Checklist。Phase 10 未改变 `phone_agent/` 业务运行时。

## RALPLAN-DR 摘要

### Principles

1. **TraeCLI-native 优先**：移植 OMC Autopilot 蓝图的编排语义，而不是迁移 `.omc` 目录、TypeScript runtime 或外部 pipeline runner。
2. **项目级 stage agents 显式化**：明确选择新增项目级 Autopilot stage agents，并以 `.trae/agents/*.md` frontmatter、名称唯一性与 hook prompt 引用测试守住契约。
3. **复用 RALPLAN 三件套**：规划阶段继续复用 `.trae/agents/planner.md`、`.trae/agents/architect.md`、`.trae/agents/critic.md`，不得复制第二套 planner/architect/critic。
4. **主 Agent 保持最终责任**：stage agents 可辅助执行、调试、测试、设计或审查；主 Agent 负责最终编辑合并、状态推进、QA 汇总与 `AUTOPILOT_SIGNAL` 输出。
5. **不触碰业务代码**：本 Phase 仅允许 `.trae/` Autopilot 相关配置/协议/agent/command/hook 与对应 `tests/trae/` 测试变更，不修改 `phone_agent/`、`main.py`、业务文档或打包入口。

### Decision Drivers

1. **Architect/Critic findings 指向 agent 策略不明确**：Phase 10 必须从“按需新增或调整 agent”收敛为“新增项目级 Autopilot stage agents”的确定策略，并列出确切文件。
2. **hook prompt 是实际编排载体**：必须让 hook prompt、agent allowlist、rule、skill、command 的关键词和 stage 语义一致，避免用户入口与运行时指令漂移。
3. **QA 需要辅助但不能转移责任**：test-engineer/code-reviewer 可协助测试与审查，但最终状态、测试结论、阻塞说明和 completion signal 仍由主 Agent 汇总。

### Viable Options

| 方案 | 优点 | 缺点 |
|---|---|---|
| A. 仅增强 hook prompt，不新增 stage agents | 文件变更少，短期实现快 | 角色契约仍埋在 prompt 中，无法做 agent frontmatter、名称唯一性、引用一致性等静态验收；不满足 findings |
| B. 新增项目级 Autopilot stage agents，并复用 RALPLAN 三件套 | 契约清晰、可测试、与 TraeCLI-native 自定义 subagent 模型一致；满足 findings 的确切文件与职责映射要求 | 新增 `.trae/agents/` 文件较多，需要同步 hook/rule/skill/command/tests 防止漂移 |
| C. 修改 `.trae/traecli.yaml` 注册或重配 agents | 可能让配置入口更显式 | 当前 findings 只要求项目级 agent 文件和 hook prompt 引用；若现有 TraeCLI 自动发现 `.trae/agents/*.md`，改 yaml 会扩大风险且不必要 |

### Selected Approach

选择 **方案 B：新增项目级 Autopilot stage agents，并复用现有 RALPLAN `planner` / `architect` / `critic` 三件套**。

Phase 10 执行时新增以下确切项目级 Autopilot stage agent 文件：

- `.trae/agents/executor.md`
- `.trae/agents/debugger.md`
- `.trae/agents/test-engineer.md`
- `.trae/agents/designer.md`
- `.trae/agents/code-reviewer.md`
- `.trae/agents/security-reviewer.md`

同时明确复用以下既有 RALPLAN agent 文件，不复制、不改名、不新增替代品：

- `.trae/agents/planner.md`
- `.trae/agents/architect.md`
- `.trae/agents/critic.md`

`.trae/traecli.yaml` **不需要修改**：本 Phase 以 `.trae/agents/*.md` 项目级 agent 文件和 `.trae/hooks/autopilot.py` stage prompt 引用作为编排契约；除非执行时发现 TraeCLI 当前版本无法自动发现项目级 agents，否则不得扩大到 yaml 配置变更。

### Stage→Agent→路径→权限/读写→职责映射

| Autopilot Stage | Agent | 路径 | 权限/读写 | 职责 |
|---|---|---|---|---|
| ralplan | planner | `.trae/agents/planner.md` | 仅规划；只允许整体更新 `.trae/rules/graph.mdc` | 生成/修订 roadmap，维护 RALPLAN Status、Phase、约束 Checklist |
| ralplan | architect | `.trae/agents/architect.md` | 只读 | 串行审查 planner 输出、架构可行性、边界与权衡 |
| ralplan | critic | `.trae/agents/critic.md` | 只读 | 串行质量门，输出 APPROVE / ITERATE / REJECT；APPROVE 前禁止实现 |
| execution | executor | `.trae/agents/executor.md` | 可写；仅按已批准计划修改允许路径 | 执行低风险实现任务、保持 diff 聚焦、不得 commit、不得清理用户无关改动 |
| execution | designer | `.trae/agents/designer.md` | 默认只读，可产出设计建议；需主 Agent 落地写入 | 在复杂实现前辅助拆分接口/流程/配置设计，避免偏离已批准 roadmap |
| execution / ralph | debugger | `.trae/agents/debugger.md` | 可读测试/日志；修复建议交主 Agent 或按 stage 授权窄写 | 定位失败根因、提出最小修复路径，不扩大重构范围 |
| ralph / qa | test-engineer | `.trae/agents/test-engineer.md` | 可读为主；可按主 Agent 指令新增/调整测试 | 设计并运行验证、补充回归测试建议、报告环境阻塞；QA 阶段辅助但不负责最终汇总 |
| ralph / qa | code-reviewer | `.trae/agents/code-reviewer.md` | 只读审查 | 审查 diff 的逻辑、健壮性、可维护性与范围边界；QA 阶段辅助但不负责最终汇总 |
| ralph / qa | security-reviewer | `.trae/agents/security-reviewer.md` | 只读审查 | 审查 prompt/hook/config 的权限、秘密、命令注入、越权写入风险 |
| qa | 主 Agent | 当前主会话 | 可写状态文件；按批准计划收尾 | 汇总 test-engineer/code-reviewer/security-reviewer 发现，决定是否修复或阻塞，输出最终状态和 `AUTOPILOT_SIGNAL` |

### ADR

- **Decision**：Phase 10 采用“新增项目级 Autopilot stage agents + 复用 RALPLAN 三件套 + hook/rule/skill/command/tests 一致性验证”的 TraeCLI-native 编排方案。
- **Drivers**：满足 Architect/Critic findings；让 stage 角色契约从 prompt 文本升级为可静态验收的项目级 agent 文件；保持 RALPLAN 审查链单一；避免业务代码变更。
- **Alternatives**：仅增强 hook prompt；迁移 OMC runtime；修改 `.trae/traecli.yaml` 做额外注册。
- **Why chosen**：新增项目级 agents 能精确表达 execution/debug/test/design/review/security 分工，且可通过 frontmatter、名称唯一性、hook 引用和关键词一致性测试回归；复用 planner/architect/critic 避免规划协议双轨。
- **Consequences**：执行时必须同步新增 agent 文件、hook prompt、Autopilot rule/skill/command 和 tests；新增 agent 越多，漂移风险越高，必须以测试约束 allowlist、关键词和权限描述。
- **Follow-ups**：若执行时发现 TraeCLI agent 自动发现机制与预期不符，先记录阻塞并回到 Planner/Critic；不得擅自修改 `.trae/traecli.yaml` 或扩大配置范围。

## 当前规划 Phase

### Phase 10: TraeCLI-native Autopilot 多 subagent 编排移植

**目标**：把 OMC Autopilot 蓝图中的多 subagent 编排落到 TraeCLI 原生机制：新增项目级 Autopilot stage agents，复用 RALPLAN `planner` / `architect` / `critic`，由 Autopilot hook 在各 stage 注入可执行 prompt，rule/skill/command 对齐用户入口与约束，测试覆盖关键一致性。

**根因**：当前 Autopilot 已有 `ralplan -> execution -> ralph -> qa` 状态机，但 execution/verification/QA 的 agent 契约、读写权限、stage prompt 引用、内置 allowlist、入口文档关键词与测试验收仍不够显式；若不新增项目级 stage agents 并做一致性验证，OMC 多角色协作语义会停留在不可验证的 prompt 描述中。

**修改文件候选**：
- 新增 `.trae/agents/executor.md`
- 新增 `.trae/agents/debugger.md`
- 新增 `.trae/agents/test-engineer.md`
- 新增 `.trae/agents/designer.md`
- 新增 `.trae/agents/code-reviewer.md`
- 新增 `.trae/agents/security-reviewer.md`
- 复用但不复制 `.trae/agents/planner.md`
- 复用但不复制 `.trae/agents/architect.md`
- 复用但不复制 `.trae/agents/critic.md`
- 更新 `.trae/hooks/autopilot.py`
- 更新 `.trae/rules/autopilot.mdc`
- 更新 `.trae/skills/autopilot/SKILL.md`
- 更新 `.trae/commands/autopilot.md`
- 更新 `tests/trae/test_autopilot_hook.py` 或同等 TraeCLI Autopilot hook 测试文件

**明确不修改**：
- 不修改 `.trae/traecli.yaml`。
- 不修改 `phone_agent/`、`main.py`、`setup.py`、`README.md`、`docs/` 或其他业务代码/产品文档。

**执行步骤与验收标准**：

1. **新增并固化项目级 Autopilot stage agents**
   - 根因：execution/verification/QA 角色边界不能只依赖 hook prompt 自然语言，需要项目级 agent 文件承载名称、职责和权限契约。
   - 修改文件候选：`.trae/agents/executor.md`、`.trae/agents/debugger.md`、`.trae/agents/test-engineer.md`、`.trae/agents/designer.md`、`.trae/agents/code-reviewer.md`、`.trae/agents/security-reviewer.md`；复用 `.trae/agents/planner.md`、`.trae/agents/architect.md`、`.trae/agents/critic.md`。
   - 验收标准：新增 6 个 agent 文件均存在且 frontmatter 合法；所有 `.trae/agents/*.md` agent 名称唯一；`planner` / `architect` / `critic` 保持 RALPLAN 串行链路且不被复制；每个新增 agent 明确读写权限、职责、不得主动 commit、不得清理用户无关改动、不得越过已批准 roadmap。

2. **升级 Autopilot stage prompt 与 allowlist 编排语义**
   - 根因：`.trae/hooks/autopilot.py` 的 stage prompt 是实际续跑指令，需要显式引用新增 stage agents、保留内置/既有 RALPLAN allowlist，并明确主 Agent 最终责任。
   - 修改文件候选：`.trae/hooks/autopilot.py`。
   - 验收标准：RALPLAN stage 明确串行复用 `planner -> architect -> critic`，ITERATE 回 planner，Critic APPROVE 前不得实现；execution stage 明确可调用 `executor`、必要时 `designer` / `debugger`，主 Agent 负责最终编辑合并；RALPH/verification stage 明确可用 `debugger`、`test-engineer`、`code-reviewer`、`security-reviewer` 做诊断/只读审查，主 Agent 修复高置信问题；QA stage 明确使用 `test-engineer` / `code-reviewer` 辅助测试与审查，但主 Agent 负责最终状态汇总、阻塞说明和 `AUTOPILOT_SIGNAL`；hook prompt 引用的 agent 与新增 agent/保留内置 allowlist 一致。

3. **同步 Autopilot rule、skill、command 关键词与边界**
   - 根因：rule/skill/command 若与 hook prompt 不一致，会导致自然语言触发、slash command、`--use-current-plan`、stage agent 引用和 runtime 行为漂移。
   - 修改文件候选：`.trae/rules/autopilot.mdc`、`.trae/skills/autopilot/SKILL.md`、`.trae/commands/autopilot.md`。
   - 验收标准：三处均声明 TraeCLI-native、不迁移 `.omc` runtime；关键词与 hook 使用的 stage 名称和 agent 名称一致；明确新增 6 个 stage agents 与复用 `planner` / `architect` / `critic`；明确 `.trae/traecli.yaml` 不需要修改；明确 mode registry、subagent tracking、completion signal、handoff、无主动 commit、无业务代码变更边界。

4. **补充 hook 与 agent contract 回归测试**
   - 根因：prompt contract、agent frontmatter 和文档关键词是 Autopilot 编排的主要执行载体，缺少测试会使后续重构 silently 破坏多 subagent 编排。
   - 修改文件候选：`tests/trae/test_autopilot_hook.py` 或同等 TraeCLI Autopilot hook 测试文件。
   - 验收标准：测试覆盖 `.trae/agents/*.md` frontmatter 合法性；测试覆盖 agent 名称唯一性；测试覆盖 hook prompt 引用与新增 agent/保留内置 allowlist 一致性；测试覆盖 rule/skill/command/hook 的 stage 关键词和 agent 关键词一致性；测试覆盖 RALPLAN prompt 的 planner/architect/critic 串行顺序和 APPROVE 门禁；测试覆盖 QA prompt 中 test-engineer/code-reviewer 仅辅助、主 Agent 最终汇总；`--use-current-plan`、stage signal、subagent active suppression、mode conflict 等既有测试继续通过。

5. **执行前后范围与验证门禁**
   - 根因：本任务是 TraeCLI 编排迁移，若误改 `phone_agent/` 或 `.trae/traecli.yaml`，会扩大风险并违反用户边界。
   - 修改文件候选：仅上述新增/更新的 `.trae/agents/`、`.trae/hooks/autopilot.py`、`.trae/rules/autopilot.mdc`、`.trae/skills/autopilot/SKILL.md`、`.trae/commands/autopilot.md`、`tests/trae/test_autopilot_hook.py`。
   - 验收标准：目标测试命令 `.venv/bin/pytest tests/trae/test_autopilot_hook.py -q` 通过；必要时补充 `python -m py_compile .trae/hooks/autopilot.py` 或等价语法检查；最终 diff 不包含 `.trae/traecli.yaml`、`phone_agent/`、`main.py`、`setup.py`、`README.md`、`docs/` 的变更；若测试环境缺依赖，需记录阻塞与已尝试命令，不伪造通过。

# 约束 Checklist

修改 `.trae/` Autopilot 编排、`phone_agent/graph/`、评测、trace、checkpoint、grounding、memory 或相关测试前，确认：

- [ ] **RALPLAN**：当前 Phase 10 已完成，后续新 Phase 仍需 `design_status=critic_approved`、`last_critic_verdict=APPROVE`、`approved_for_execution=true` 三者一致并完成 Final Check 后才能实现；Phase 完成后先更新本文件，再按用户要求决定是否 commit。
- [ ] **本 Phase 范围**：只允许修改 `.trae/agents/executor.md`、`.trae/agents/debugger.md`、`.trae/agents/test-engineer.md`、`.trae/agents/designer.md`、`.trae/agents/code-reviewer.md`、`.trae/agents/security-reviewer.md`、`.trae/hooks/autopilot.py`、`.trae/rules/autopilot.mdc`、`.trae/skills/autopilot/SKILL.md`、`.trae/commands/autopilot.md`、`tests/trae/test_autopilot_hook.py` 或同等 TraeCLI Autopilot hook 测试文件；不得修改 `phone_agent/`、`main.py`、`setup.py`、`README.md`、`docs/` 或其他业务代码。
- [ ] **`.trae/traecli.yaml` 边界**：本 Phase 不需要修改 `.trae/traecli.yaml`；若执行时发现 agent 自动发现不可用，记录阻塞并回到 Planner/Critic，不擅自扩大范围。
- [ ] **Autopilot TraeCLI-native**：不迁移 `.omc`，不引入 TypeScript pipeline runtime，不新增第二套 runtime state；runtime state 仍在 `.trae/autopilot/state.json`，mode registry 仍在 `.trae/modes/state.json`。
- [ ] **subagent 编排**：RALPLAN stage 复用 `.trae/agents/planner.md` / `.trae/agents/architect.md` / `.trae/agents/critic.md` 且串行；新增项目级 stage agents 为 `executor`、`debugger`、`test-engineer`、`designer`、`code-reviewer`、`security-reviewer`；execution/team 可并行 subagent，但主 Agent 负责最终编辑、合并与一致性；verification/RALPH 优先只读审查，主 Agent 修复高置信问题；QA 使用 test-engineer/code-reviewer 辅助但主 Agent 负责最终状态汇总。
- [ ] **agent contract 测试**：必须验证 `.trae/agents/*.md` frontmatter 合法性、agent 名称唯一性、hook prompt 引用与新增 agent/保留内置 allowlist 一致性、rule/skill/command/hook 的 stage 与 agent 关键词一致性。
- [ ] **Completion Signal 与 stop hook**：每个 stage 完成后仍必须单独输出 `AUTOPILOT_SIGNAL: <SIGNAL>`；hook 只从 assistant transcript 识别完成信号；subagent 活跃窗口内不得注入新的 continuation prompt。
- [ ] **Mode / handoff**：尊重 `.trae/modes/state.json` 互斥锁；`/autopilot --use-current-plan` 只能在 `graph.mdc` 已 approved 或 RALPLAN pending approval 可 handoff 时从 execution 开始。
- [ ] **Graph 不变量**：State schema 先行；不用 while loop 替代 StateGraph；不恢复 `_execute_step` 双轨；Node 不使用 `self._context`，上下文走 State `messages` + config。
- [ ] **依赖注入**：DeviceFactory / ModelClient 通过 StateGraph config 注入；tool 不直接 `get_device_factory()`；`device_factory` 不得进入 `@tool` 模型可见 schema，需有 fake-device 与 schema 断言测试。
- [ ] **动作安全**：坐标保持 0-1000 相对值，绝对像素转换只在 execute/tool 内调用 `convert_relative_to_absolute()`；动作解析用 `ast.parse` + `ast.literal_eval`，禁止 `eval()`。
- [ ] **上下文与 reducer**：每步后剥离历史图片；`plan_node` 只返回新增 messages，`execute_node` 返回完整重建列表；role/content 边界必须有测试。
- [ ] **HITL / confirm-then-execute**：敏感操作走 `confirm_node`，登录/验证码走 `takeover_node`，均用 `interrupt()`；当前只承诺 interrupt 路由语义，持久 resume 等后续 checkpoint/resume Phase 完成后再对外承诺。
- [ ] **pending_execute 约束**：`after_interrupt` 仅在 `pending_execute=True & interrupt_result=True` 时到 `execute`；pending 分支不得 `_strip_and_append`，必须设置 `action_confirmed=True` 并保留 `action_parsed`；`after_execute` 先检查 `action_confirmed`；`plan_node` 每轮重置 `action_confirmed=False`；`confirm_node` 显式返回 `pending_execute`。
- [ ] **Phase 6 门禁**：保留 `setup.py` 并补 runtime deps；`.gitignore` 不得忽略 `tests/`；测试/文档不得声明未经验证的 passing 数；README 拓扑必须与 `builder.py` 一致。
- [ ] **Eval / Trace**：`PhoneAgent.run() -> str` 保持兼容；结构化评测使用 `run_structured()` 或等价参数；Evaluation Harness 至少记录 success/steps/duration/error/HITL count/trace_id/trace_path；Trace 默认本地可用，LangFuse 可选且未配置不影响主流程。
- [ ] **Reflection 策略**：`reflect_node` 输出/解析结构化 verdict、failure_cause、suggested_strategy；解析失败必须安全降级；Plan 下一轮必须能看到结构化 reflection；不通过模型动态改图拓扑。
- [ ] **隐私与收益验证**：Trace/checkpoint/memory 默认不得上传或长期保存敏感截图/API key/隐私文本；Reflection、grounding、memory 的收益必须通过 Evaluation Harness 对比验证。
- [ ] **后续能力边界**：checkpoint/resume 先于 UI grounding，且不得绕过 HITL；Accessibility/OCR 不破坏 Android 主线和坐标转换约束；Memory 不得绕过 HITL/安全确认/当前设备状态检查；TraeCLI Autopilot subagent 编排不得被误当作 `phone_agent` 业务多 Agent 架构。
- [ ] **暂不做**：Dreaming、`phone_agent` 业务多 Agent 架构、Command 动态路由、复杂 Prompt 外部化、iOS 主线、早期技能记忆。
