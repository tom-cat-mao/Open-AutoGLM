---
description: LangGraph roadmap 与核心执行约束；仅在 RALPLAN/Autopilot 规划阶段加载
alwaysApply: false
globs: ".codex/skills/ralplan/SKILL.md,.codex/skills/autopilot/SKILL.md,.codex/skills/ultragoal/SKILL.md,.codex/skills/code-review/SKILL.md,.codex/skills/ultraqa/SKILL.md,.trae/rules/ralplan.mdc,.trae/rules/autopilot.mdc,.trae/rules/graph.mdc"
---

# RALPLAN Status

```yaml
status: completed
design_status: critic_approved
execution_status: completed
iteration: 1
planner_mode: null
revision_scope: null
last_planner_update: "PhoneAgent goal contract rebuild completed from RALPLAN-approved design."
last_architect_review: "Architect and Critic approved GoalContract + GoalEvaluator + finish validation gate rewrite."
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "Declarative GoalContract compiled once by goal_node; criterion evidence settles layer by layer from the ledger (seal > observed > judge reference), judge satisfied requires a valid evidence_step; GoalEvaluator applies fail-closed unknown→replan. Hybrid Accessibility + LocateAnything remains default grounding."
```

# Roadmap

## 已完成 Phase

- LangGraph PhoneAgent core loop、ExpectedOutcome、Context Harness、structured JSON/tool_calls adapter、ActionIR Validator/Repair/Safety Gate、trace/checkpoint redaction 已进入代码事实。
- Grounding 当前代码事实：`MarkProvider` 是单个 provider 的 Protocol；runtime plan/reflect 主路径使用 `build_mark_providers()` 注入 `list[MarkProvider]` 到 `build_observation()`；默认 `PHONE_AGENT_GROUNDING_PROVIDER=hybrid` 由 `build_mark_providers()` 返回 `FallbackMarkProvider([AccessibilityTreeProvider, LocateAnythingMLXProvider])`。
- `build_mark_provider()` 单数 factory 仍存在，但只适合 legacy/single-provider fallback；它不处理 `hybrid`，不应作为新能力主接入口。
- Accessibility Tree hybrid、ScreenStructure/ObjectRegistry、object selector grounding、LocateAnything structured visual sidecar、hybrid_factory/fallback_chain 诊断已完成。
- Grounding benchmark 已在 `bench/grounding/` 建立：manifest 使用 0-1000 bbox；scoring/reporting 可离线复评 predictions；本地 runner 为 `run_locateanything.py`。
- Pseudo-success 防护已完成（原始层）；Goal Contract Rebuild 在其之上推倒重来：
  - 旧 `TaskGoalContract`（关键词分类 + `validate_finish_claim()` 中文 contains 匹配）已删除，包含 `task_goal.py` 与 `test_task_goal.py`。
  - 新 `GoalContract` + `SuccessCriterion[]` 在 `phone_agent/graph/goal.py` 定义：声明式字段 `redacted_objective`/`success_criteria`/`constraints`/`non_goals`/`verification_strategy`/`stop_conditions`/`compile_status`/`compile_source`/`target_app_hint`/`ordinal`。
  - `GoalContractCompiler` (`goal_compiler.py`) 编译链：External（benchmark 注入）> LLM（step 0 一次结构化调用，有 `compile_retry`，失败回退）> Heuristic（永不失败的兜底，所有 criterion 为 `vlm_judge`，不驱动自动成功）。
  - `goal_node` 在拓扑 `START → goal_node → plan_node` 中编译一次；已编译非 `needs_recompile` 时 no-op；可选 `require_goal_approval` interrupt 让用户 Approve/Edit（与 confirm_node 同模式）。
  - `FinishActionDict` 新增 `matched_terminal_evidence: list[str]`，由 adapter 校验类型；该列表是模型的 finish 声明（claim），仅作 trace 诊断进入 `finish_claim_matched`/`unknown_finish_claim_ids`，不再直接改变判定——判据由证据账本分层坐实（seal > model_observation > judge 引用），命名本身不产生 matched/missing（claim 命名门禁已退役）。
  - `GoalEvaluator` (`goal_evaluator.py`) 按 verification 分发：`accessibility_text_match`（sha256 stub+`_match_expected_text`）、`object_hash_match`/`object_rank_match`（读 `verifier_evidence.selected_object_signals`）、`app_or_activity_match`（复用 `_package_for_app_name`）、`focus_or_keyboard`（复用 `_focus_signals`）、`vlm_judge`（judge 的 satisfied 必须携带合法 evidence_step 引用；reflect JSON `named_evidence` 含 grounded `screen_reference` + 程序化反证优先；finish claim 命名门禁已退役）、`external_probe`（benchmark 注入 callable）。
  - status：所有 required matched 且无 required missing → success；任一 required 显式 missing → failure；无显式 missing 但未全覆盖 → unknown（**不自动升级 success**）。tentative `task_goal_contract.goal_type=="generic_task"` 的 `unknown→success` 后门已删除。
  - Reflect 增加 context 窗口管理：`select_reflect_context` + `build_reflect_context_block`（与 plan 同源更窄，注入最近 K=3 action_outcome 摘要作为 trajectory 记忆）；`goal_contract_block` 提到独立 message 块（不嵌入 dynamic user），利 prompt prefix cache。
- PhoneAgent generalization Phase 1 已完成：
  - `AppRegistry` 将 legacy launch names 聚合为 package-unique identities；alias 冲突显式 ambiguous，unknown package 不再映射为 `System Home`。
  - observation identity、installed inventory、LaunchPolicy/LaunchTargetResolver 已分离；legacy `APP_PACKAGES` 仅保留为兼容数据源。
  - `DeviceFactory.capture_observation()` 使用 foreground-before → screenshot → foreground-after 复合采样，成功后分配单调 epoch；竞态耗尽返回 `observation_unstable` 并 fail-closed。
  - Plan/Reflect 的 ScreenSnapshot/MarkRegistry 已绑定稳定 epoch 与 foreground package/activity；GoalCompiler 删除五 App `APP_HINTS` 与固定 prompt enum，统一消费 AppRegistry。
- PhoneAgent generalization Phase 2 已完成：
  - 每个 canonical action 都有不可变 `ToolCapability` 声明；Validator 检查声明存在，Executor 在 Safety Gate 后、dispatch 前执行 implementation availability 门禁。
  - Execute 产生 dispatch-only `ActionReceipt`；`accepted` 不代表 transition 或 Goal 成功，`action_succeeded` 只由 Reflect 写入。
  - `Note`/`Call_API` unavailable fail-closed，`Interact` delegated 走 takeover；所有 UI/external action 与 `Wait` 强制 Reflect reobserve，旧 action-name skip list 已删除。
  - H2 execute hardening：confirm 接受后、dispatch 前做 fresh-hash 校验——有 grounded mark 且 `MarkRegistry` 在场时，抓一帧新截图与 `raw_screenshot_hash` 比较；registry hash 缺失（旧 checkpoint/手工构造）、抓帧失败或不匹配一律 stale → fail-closed replan（`confirm_stale_reobserve`，不 dispatch）；无 mark 绑定（Launch/Back/Wait…）跳过校验。终局分支（capability missing/unavailable、terminal reject）用 `_terminal_messages` 把历史瘦化为单行 `sN: …`；`_strip_think_from_history` 对历史 assistant answer 超过 500 字符（`ANSWER_TRUNCATION_LIMIT`）截断并追加省略标记。takeover/delegated 内联分支与终局分支一样清空 `pending_execute`/`interrupt_result`，stale pending 不得路由到 confirm/takeover/reflect（P0 #5）。
- PhoneAgent generalization Phase 3 已完成：
  - `PredicateCatalog` 为 core predicate 声明 value schema、allowed sources、Matcher 与强制 privacy/state/trace/checkpoint projection；未知 predicate、任意 matcher token 和未授权 whole-screen source fail-closed。
  - `ExpectedTransition`、neutral `ObservedFact`、evidence binding、`Matcher` 与 `EvidenceAuthorityPolicy` 已建立；同层冲突为 unknown，低置信度或 stale/cross-contract fact 不参与 resolution。
  - `RuntimeObservationContext` 只在当前 node 持有 screenshot，绑定 screen/epoch、可失效且禁止序列化；Plan/Reflect 不再把 screenshot base64 写回 AgentState/message history，当前截图只进入当次 VLM request。
  - legacy `ExpectedOutcome` 保留 verifier 兼容行为，同时产生不含 runtime expected value 的 typed shadow；toggle/form/collection/dialog/navigation/content-change generic semantics 已有离线契约测试。
- PhoneAgent generalization Phase 4 已完成：
  - `TaskRequirementSet` 从 raw task 独立提取并绑定 source-span 摘要；`ContractAdequacyValidator` 在 schema validation 后核对 task/app/ordinal/entity/typed semantic coverage，模糊或不支持语义 fail-closed。
  - `CriterionSpec` 以 `PredicateSpec` 为 typed truth contract；legacy deterministic app/rank/focus/text-hash criterion 通过显式 migration 进入 typed 路径，production external override 必须同时提供独立且 task-bound 的 requirement set。
  - Reflect 独占写入 bounded、privacy-safe `goal_evidence_ledger`；`PureGoalEvaluator` 只折叠 contract + ledger + current screen/epoch binding + finish criterion IDs，不调用 model/device/provider。
  - unknown claim ID、遗漏 criterion、stale/cross-contract evidence、semantic contradiction 均阻止 finish；Goal objective/criterion/constraint/non-goal 文本在 prompt/state 边界统一 regex-redact。
- PhoneAgent generalization Phase 5 已完成：
  - `FactCollector` 从 device/accessibility/object/mark core providers 及受控 visual-region/whole-screen/external-probe providers 收集中性 facts，统一执行 schema、provider version、contract/screen/epoch binding 与 bounded collection 门禁。
  - Reflect 每轮 reobserve 收集 typed facts 并写入 privacy-safe Goal ledger；pending finish 只折叠已绑定 ledger，provider/extractor 失败或缺失为 unknown。
  - `OptionalAdapterRegistry` 只能追加 neutral fact providers，不能修改 catalog/matcher/authority/safety/edges/success；高权威 programmatic contradiction 优先，同层冲突仍为 unknown。
  - feed/player 词表已移至 optional legacy page-signal compatibility adapter；adapters disabled 的 Silverstone/Singapore 离线 replay 仍经 collector → Matcher → authority → Goal fold 正确拒绝错内容。
- PhoneAgent generalization Phase 6 已完成：
  - versioned `SafetyPolicyRegistry` 集中拥有中英安全词、semantic tags 与固定 precedence；NFKC/token-aware 匹配覆盖 adversarial 变体，credential/captcha takeover 高于 payment/privacy confirm。
  - immutable named `VerificationPolicy` 集中 mark confidence、perceptual hash distance、fact confidence、deterministic reflection 与 takeover retry thresholds；每项含 owner/unit/rationale/calibration dataset+version/fail-closed boundary。
  - offline calibration 只报告 false accept/reject，不运行时调参；ordinary unknown page 不自动 HITL，可能敏感副作用的 classification uncertainty 才 fail-closed confirm。
- PhoneAgent generalization Phase 7 已完成：
  - `TrustedGoalResumeBinder` 用 HMAC-SHA256 绑定 canonical runtime TaskRequirementSet + GoalContract（含 typed expected values）+ schema/normalizer versions；token 不替代事实且 secret 不进入 state/checkpoint。
  - 只允许 `scope=trajectory` 且 predicate persistence=`checkpoint_safe` 的 bounded progress 投影；terminal visual 与 runtime-only semantic evidence resume 后一律 stale/丢弃。
  - missing projection、wrong key、version/contract/value mismatch 均返回 `goal_contract_invalid`、空 ledger 并要求 reobserve；普通 serializer 删除 unbound Goal progress 与低熵 task/entity hashes。
  - runtime checkpointer 尚未接入 PhoneAgent/builder/CLI，activation 保持显式 gated deliverable。
- PhoneAgent generalization Phase 8 已完成离线范围：
  - `RuntimeGoalContext` 作为每次 run 显式依赖持有完整 contract/requirements；AgentState 只保留随机 opaque reference 和类型/计数元数据，绑定缺失或错配 fail-closed。
  - task binding 统一使用 NFKC + casefold + whitespace canonicalization；CJK 紧邻 Latin app alias 可解析且仍阻止 Latin 子串误匹配。
  - optional fact provider 由 core-owned registration 固定 provider ID/version、allowed sources 与 authority ceiling；adapter 冒充 accessibility/device fact 会被拒绝。
  - legacy feed/player classifier 默认关闭且只写 shadow telemetry；它不能改变 `VerifierResult`、`action_succeeded`、Goal evidence 或 routing。
  - Graph 的 UIAutomator dump 只经 `DeviceFactory.dump_uiautomator_xml()`；Plan/Reflect 不访问 device module。
  - state/trace/checkpoint egress 的 Goal/requirement/private-text 投影只保留类型、计数、长度、状态和 opaque runtime reference；禁止低熵普通 SHA。trusted resume HMAC 是唯一持久语义绑定。
  - prompt-side content projection 曾通过 `projection.py` 试行，后按 `.omx/plans/privacy-prompt-projection-rollback-20260726.md` 回滚：截图通道已携带完整内容，因此该机制既损害性能，也不符合威胁模型。Prompt 仅保留高风险 regex 脱敏与 password 字段源头抑制；持久化隐私边界仍在 state/trace/checkpoint egress。
  - cross-domain、negative、metamorphic、privacy、performance、rollout/rollback 与全量 P0 离线回归已覆盖；Phase 9 真机 gate 明确延期，不从离线结果推断。
- Verification layer hardening 已完成：
  - Predicate evidence scope 按 `(predicate, source)` 解析；accessibility existential 任一命中即 `matched`，当前视口未见为 `unknown/not_observed_in_view`，summary source 的正面反证仍可 `contradicted`。
  - `element_scoped` 的 `ui.toggle_state` / `ui.object_rank` 在多元素屏幕保持 fail-closed `unknown`；在证据绑定 selected element 前不做 existential fold。
  - compiler 只从引号 literal 或通过 shape guard 的 entity span 绑定 raw text；`vlm_judge` 不再绑定 compiler substring。连续未见 binding 只产生 `binding_never_observed` degraded 诊断，不 hard-veto。
  - `observation_retry_count` 仅统计 observation infrastructure failure，`acceptance_round_count` 仅统计 finish rejection；旧 writable `retry_count` 已删除，RunResult 只保留二者之和的兼容派生值。
  - `trajectory_liveness` 纯折叠 criterion movement 与新 `(surface, screen_id)` 状态，输出 `advancing|exploring|stuck`；surface/hash/marks/typed text 变化不算 Goal progress。stuck 先 replan，max_steps 只报告 incomplete，takeover 仅用于 human-required 或不可恢复 observation failure。
- 仍需保留既有 hardening 未闭环项：hint-mismatched provider marks 不得进入 executable `marks`、diagnostic code 字段需 strict sanitizer、hybrid child `parse_summary` 汇总、accessibility failure taxonomy 同步。

## 约束 Checklist

- [x] `MarkProvider` Protocol 不废弃；新增 provider 仍实现该单体接口。
- [x] Runtime/provider composition 主路径必须是 `build_mark_providers()`。
- [x] 默认 `PHONE_AGENT_GROUNDING_PROVIDER=hybrid`（AccessibilityTree + LocateAnythingMLX fallback）。
- [x] `target_mark_id -> MarkRegistry -> ActionIR` 是唯一执行边界；grounding 只生成 marks，不直接执行 tap。
- [x] `target_text_hint` 只能作为 provider hint。
- [x] Grounding failure fail-closed；多候选、无候选、越界、格式错不回退主 VLM 坐标。
- [x] Tree-first: Accessibility Tree 为 hybrid 第一层；解析失败、hint 不匹配、provider 缺失不能回退到主 VLM raw coordinate。
- [x] trace/checkpoint 不含 raw screenshot、raw hint、API key、raw response 或低熵语义摘要；metadata 只允许 code/count/bool/length/latency/model、屏幕绑定摘要和 opaque runtime reference，持久语义绑定只允许 keyed HMAC。
- [x] `finish` 不是执行成功；必须经过 GoalContract `SuccessCriterion` evidence gate（判据由账本逐层坐实：seal > 账本 observed > judge 引用；judge 的 satisfied 必须携带合法 evidence_step 引用），未坐实且无引用=unknown → fail-closed/replan；unknown 不自动升级 success。
- [x] `evidence_scope` 决定 authority fold；existential absence 为 unknown，positive counter-observation 才能 contradicted。
- [x] per-step verifier、finish acceptance、trajectory liveness 三问分属不同 owner；single-step verdict 不驱动 stuck/takeover。
- [x] `bench_output/`、API key、runtime traces、真实截图数据默认不提交。
