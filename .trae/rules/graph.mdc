---
description: LangGraph roadmap 与核心执行约束；仅在 RALPLAN/Autopilot 规划阶段加载
alwaysApply: false
globs: ".trae/commands/ralplan.md,.trae/commands/autopilot.md,.trae/hooks/ralplan.py,.trae/hooks/autopilot.py,.trae/rules/ralplan.mdc,.trae/rules/autopilot.mdc,.trae/rules/graph.mdc"
---

# RALPLAN Status

```yaml
status: critic_approved
design_status: critic_approved
execution_status: completed
iteration: 1
planner_mode: null
revision_scope: null
last_planner_update: "Architect feedback applied: ExpectedOutcome separated from ActionIR, after-action observation scope expanded, VLM verifier isolation and existing routing reuse clarified."
last_architect_review: "Architect requested targeted revision around ExpectedOutcome boundary, after-action observation files, VLM verifier isolation, routing fit, and smoke test scope."
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "Autopilot implementation completed; final Critic ITERATE findings addressed for delivery boundary, before/after verifier context, focus/keyboard signals, tests, and docs."
```

# Roadmap

## 已完成 Phase

- Phase V1-V5 已完成：新增 `ExpectedOutcome` sibling verification contract、provider envelope 解析、动作后 observation 重建、deterministic postcondition verifier、unknown/低置信时的 isolated verifier request、trace-safe verifier evidence、targeted regression tests 与 README/docs/AGENTS 同步。旧 action JSON 兼容；真实 `ModelClient` JSON schema/auto 路径保留 envelope 供 Plan 拆分；`ExpectedOutcome` 不进入 canonical ActionIR / Validator / Safety / Executor；state 内保存 runtime hash/哨兵 contract，`action_raw`/trace/report/checkpoint 使用 stub/hash summary；`screen_changed` 仅作为 weak signal，不再单独证明 Tap/Type/Search/Video/Swipe 成功；`input_focused` 会结合 focused/editable/keyboard/top activity、输入文本、Search/搜索按钮等只读信号；VLM verifier request 使用 stub/hash before/after observation summary 且不追加 `state["messages"]`；只有按 action/outcome 绑定的 `strong_progress` 会阻止机械 takeover；Reflect 默认 accessibility-only，不触发 LocateAnything fallback，除非显式开启。

## RALPLAN-DR 摘要

### Principles

1. **Postcondition over pixel change**: 动作验证必须验证预期状态，而不是把全屏截图变化当作成功。
2. **Deterministic first, VLM when uncertain**: 优先用 UI tree、focus、keyboard、foreground app、ADB returncode 等稳定信号；无法判定时再调用主 VLM verifier。
3. **ExpectedOutcome is ActionIR's sibling contract**: 计划阶段必须为可验证动作生成结构化后置条件，但它不进入 canonical ActionIR、Validator/Safety/Executor 的执行 payload。
4. **Dynamic UI is noise by default**: B 站首页、广告、banner、推荐流、热词、计数器等动态区域不得单独作为成功信号。
5. **Fail closed on unverifiable critical progress**: 搜索、输入、打开结果、播放视频等关键步骤若无法验证目标状态，必须 retry/replan/takeover，而不是继续假定成功。

### Decision Drivers

1. 实机测试证明 `screen_changed=true` 会在 B 站动态首页误判 Tap 成功；验证目标需要升级为 action-specific postcondition。
2. 现有 `verify_action_outcome()` 只支持 Launch app match、execution failure、screen hash changed，无法验证搜索页/输入框/结果页/播放页。
3. 主 VLM 已有视觉理解能力，但当前 Reflect prompt 缺少 before/after tree、目标 mark、expected outcome 和动态噪声规则，导致反思不稳定且不够可执行。

### Viable Options

| Option | Pros | Cons |
|---|---|---|
| A. Keep current verifier and improve prompts only | 改动小；短期可能减少部分误判 | 仍依赖 screen hash；无法强制验证输入框、搜索结果和播放页；动态 UI 仍会误导 |
| B. Deterministic postcondition verifier only | 快、稳定、可测试；不依赖模型输出格式 | 覆盖复杂视觉状态有限；跨 App 通用 schema 设计成本高 |
| C. Hybrid ExpectedOutcome + deterministic verifier + VLM verifier | 能处理 B 站等复杂动态页面；保留稳定信号；VLM 只在必要时介入 | 改动面较大；需要 ActionIR/State/Reflect/Prompt/Trace/Test 协同 |

### Selected Approach

选择 **C. Hybrid ExpectedOutcome + deterministic verifier + VLM verifier**。

目标运行策略：

```text
Plan
  -> main VLM emits provider response envelope
  -> adapter splits canonical IntentIR/ActionIR and sibling ExpectedOutcome
  -> ActionIR continues through grounding/validator/safety/executor
  -> ExpectedOutcome goes through independent validation and is only stored for verifier/trace/context
Execute
  -> dispatch tool and collect command-level result
Reflect/Verify
  -> capture screenshot + UI tree + current_app + focus/keyboard signals
  -> deterministic postcondition verifier checks ExpectedOutcome
  -> if success/failure is certain, update state
  -> if uncertain, invoke VLM verifier with current screenshot, before/after observation summaries, target mark, expected outcome, focus/keyboard signals, and dynamic-noise instructions
  -> update reflection_verdict/failure_cause/suggested_strategy/retry_count
  -> reuse existing reflect -> should_continue -> plan/end topology; takeover only through existing HITL interrupt path
```

### ADR

- **Decision**: 引入独立 `ExpectedOutcome` sibling contract 与 action-specific postcondition verifier；将当前 `screen_changed` 降级为弱信号；新增可选主 VLM verifier 作为不确定状态的语义裁判。
- **Drivers**: B 站动态首页证明截图 hash 会误判；搜索框 Tap 后必须验证输入页/focus/keyboard，而非只看画面变化；复杂任务需要上下文驱动的验证 prompt。
- **Alternatives**: 只调 prompt；只做 deterministic verifier；依赖人工 takeover；关闭动态页面测试。
- **Why chosen**: 同时解决可测试性和语义理解，最符合现有 Plan-Execute-Reflect 架构；不破坏 MarkRegistry/Safety/HITL 边界。
- **Consequences**: 需要新增 state 字段、provider response envelope 解析、独立 outcome validator、verifier 模块、reflect prompt、trace 字段和测试；短期复杂度上升。
- **Follow-ups**: 可进一步增加 per-app verifier 插件、动态区域 masking、UI tree diff、视频播放页专用判定、真实 B 站 smoke suite。

## 当前规划 Phase

当前 approved roadmap 已执行完成。下面保留原始 Phase 设计与验收矩阵作为实现依据和后续审查索引。

### Phase V1 - Verification Data Model

- **目标/根因**: 为每个动作建立可验证的后置条件。根因是当前 `verify_action_outcome()` 只看 app match / screen hash change，无法表达“搜索输入页打开”“输入框聚焦”“视频播放页出现”等目标状态。
- **修改文件候选**: `phone_agent/actions/ir.py`, `phone_agent/actions/adapter.py`, `phone_agent/actions/validator.py`, `phone_agent/graph/expected_outcome.py`, `phone_agent/graph/state.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/model/client.py`, `tests/actions/test_adapter.py`, `tests/graph/test_plan_reflect.py`。
- **计划步骤**:
  1. 定义独立 `ExpectedOutcome` 数据结构：`kind`, `must_observe`, `must_not_observe`, `target_mark_id`, `target_text_hint`, `timeout_hint`, `dynamic_regions`。
  2. 新增 `phone_agent/graph/expected_outcome.py` 或同等模块，负责 outcome validation / normalization / trace-safe summary；它不得被 executor 调用。
  3. 扩展 provider response envelope：adapter 将响应拆成 canonical IntentIR/ActionIR 与 sibling ExpectedOutcome；canonical ActionIR 继续保持现有严格字段白名单。
  4. `plan_node` 将 validated outcome 放入 state 与 trace，供 reflect/verifier 消费。
- **验收标准**:
  - 旧 action JSON 兼容；无 outcome 时 verifier 使用保守默认。
  - `ExpectedOutcome` 字段经过独立 validator；非法/危险字段 fail-closed 或降级为 conservative default，且不进入 executor。
  - Tap 搜索框、Type、Launch、Swipe、Wait 至少有默认 outcome 生成策略或 schema 支持。

### Phase V2 - Deterministic Postcondition Verifier

- **目标/根因**: 将 `screen_changed` 从成功条件降级为弱信号。根因是 B 站首页动态 banner/广告/推荐流会导致截图 hash 变化并误判动作成功。
- **修改文件候选**: `phone_agent/graph/verifier.py`, `phone_agent/graph/observation.py`, `phone_agent/graph/marks.py`, `phone_agent/graph/nodes/reflect.py`, `phone_agent/grounding/accessibility.py`, `phone_agent/grounding/provider.py`, `phone_agent/adb/device.py`, `phone_agent/device_factory.py`, `tests/graph/test_plan_reflect.py`, `tests/graph/test_execute.py`。
- **计划步骤**:
  1. 扩展 after-action observation：截图、current_app、UI tree marks、可选 focused node、editable/input node、keyboard/IME 可见性、top window/activity。
  2. 在 Reflect verifier 中复用 `dump_uiautomator_xml()`，并通过可选只读接口/config 信号接入 `get_focused_window_or_app()`、`get_top_activity()`、`is_keyboard_visible()`；接口不可用时降级为 unknown，不猜测成功。
  3. 为动作类型实现 deterministic postconditions：
     - Launch: target app/activity match。
     - Tap search bar: editable/focused input、搜索页 toolbar、取消/搜索按钮、键盘可见。
     - Type: focused/input text contains expected text。
     - Tap result/video: player/detail page stable signal。
     - Swipe: list topology/content shift，而不是 banner/hash change。
     - Wait: loading 消失或 target appeared。
  4. `screen_changed` 仅记录为 `weak_signal`，不单独返回 `success`。
- **验收标准**:
  - B 站首页动态推荐/广告变化不会让 Tap 自动成功。
  - 若点击搜索栏后仍停留首页推荐流，返回 failed/unknown，并给出 retry strategy。
  - Launch app match 仍保持高置信成功。
  - ADB 命令 returncode/stderr 可作为 hard failure 输入。

### Phase V3 - VLM Verifier Prompt And Context Harness

- **目标/根因**: 在 deterministic verifier 不确定时，调用主 VLM 做语义验证。根因是复杂 App 页面不能完全靠固定规则，但当前 Reflect prompt 缺少 ExpectedOutcome、before/after tree diff、dynamic-noise 指令。
- **修改文件候选**: `phone_agent/graph/nodes/reflect.py`, `phone_agent/graph/context.py`, `phone_agent/graph/expected_outcome.py`, `phone_agent/config/prompts_zh.py`, `phone_agent/config/prompts_en.py`, `phone_agent/model/client.py`, `phone_agent/checkpoint/serde.py`, `tests/model/test_client.py`, `tests/graph/test_context_consumer.py`。
- **计划步骤**:
  1. 新增专用 verifier prompt schema，输出 `action_effect`, `task_progress`, `matched_postconditions`, `missing_postconditions`, `dynamic_change_only`, `evidence`, `next_strategy`。
  2. 构建 verifier context：原始任务、动作、target mark、expected outcome、当前截图、before/after observation summary、dynamic region hints、deterministic verifier signals。
  3. VLM verifier 必须是 isolated request：不追加到 `state["messages"]`，不参与 request compaction 的持久状态，trace/checkpoint 不写 raw screenshots、raw hint 或 raw tree，只写 evidence ids 与 redacted summaries。
  4. 明确 prompt 规则：广告、banner、推荐流、热词、计数器变化不能作为成功证据；必须引用 postcondition evidence。
  5. 失败或 JSON parse error 时，不直接假定成功；回退到 deterministic result 的 conservative outcome。
- **验收标准**:
  - VLM verifier 输出严格 JSON，parse failure 可观测且不会误判成功。
  - 搜索框 Tap 的 VLM verifier 能区分“首页搜索栏热词变化”和“搜索输入页已打开”。
  - `context_mode=inject` 不泄露 raw private text；trace 只记录摘要与 evidence ids。

### Phase V4 - Routing, Retry, And Takeover Policy

- **目标/根因**: 让验证结果真正影响图路由。根因是当前反思失败或弱成功会继续 replan，导致复杂任务走偏直到 timeout/max steps/takeover。
- **修改文件候选**: `phone_agent/graph/nodes/reflect.py`, `phone_agent/graph/context.py`, `phone_agent/graph/state.py`, `phone_agent/graph/edges.py`（仅当复用现有拓扑无法表达时）, `tests/graph/test_edges.py`, `tests/graph/test_hitl.py`。
- **计划步骤**:
  1. 将 verifier verdict 归一为 `verified_success | verified_failure | unknown | blocked`，并映射到现有 `reflection_verdict/failure_cause/suggested_strategy/retry_count` 字段。
  2. 优先复用现有 `reflect -> should_continue -> plan/end` 拓扑；不新增条件边，除非 Final Design 明确证明必须。
  3. 对 `verified_failure` 生成 retry strategy：tap parent clickable、wait stable、type text、go back、reobserve、takeover。
  4. 对 repeated unknown/failure 设置 retry budget 和 takeover threshold，避免无限循环。
  5. 保持 terminal guard：finished/error 优先于 pending interrupts；takeover 仍只通过既有 HITL interrupt path。
- **验收标准**:
  - verified_failure 不会被 `screen_changed` 覆盖。
  - repeated failure 会进入 replan 或 takeover，而不是一直走同一 Tap。
  - HITL confirm/takeover 语义不变，敏感动作仍走 interrupt。

### Phase V5 - Tests, Trace, And Bilibili Smoke Suite

- **目标/根因**: 用测试固定新验证 contract。根因是当前问题由实机动态 UI 暴露，需要 fixture 和 smoke 防止回归。
- **修改文件候选**: `tests/graph/test_plan_reflect.py`, `tests/graph/test_edges.py`, `tests/actions/test_adapter.py`, `tests/model/test_client.py`, `tests/graph/fixtures/*`（如需新增轻量 fixture）, `README.md`, `docs/future-roadmap.md`, `AGENTS.md`, `.trae/traecli.yaml`。
- **计划步骤**:
  1. 单测覆盖：screen hash changed 但 postcondition 缺失 => unknown/failed。
  2. 单测覆盖：search bar Tap after tree 未出现 EditText/取消按钮 => failed with retry strategy。
  3. 单测覆盖：Launch app match 仍 success；Type text postcondition 成功/失败。
  4. trace 新增 verifier evidence、matched/missing postconditions、dynamic_change_only、retry_strategy。
  5. 必须项使用 fixture/fake device 固定：hash changed + postcondition missing、EditText absent、Launch success、Type text match、dynamic-only change。
  6. 手动/设备 smoke：B 站搜索栏验证、设置 WLAN 验证、动态首页不误判；不作为核心单测阻塞项。
- **验收标准**:
  - 目标单测全部通过。
  - fixture 中 B 站搜索框点击失败能被识别为未进入搜索页，而不是 success。
  - 手动 B 站 smoke 记录为 evidence，不作为无设备环境的必跑测试。
  - README/docs/AGENTS 更新验证架构与配置说明。

# 约束 Checklist

- [x] 保持坐标 0-1000；tool/backend 内转换绝对像素。
- [x] 屏幕目标点击类动作仍只能通过 `target_mark_id` grounding，禁止恢复 raw coordinate Tap。
- [x] `ExpectedOutcome` 不是执行授权，不得绕过 Validator/Safety/HITL。
- [x] `ExpectedOutcome` 不进入 canonical ActionIR 或 executor payload；它是 sibling verification contract。
- [x] `screen_changed` 只能作为弱信号，不能单独作为 Tap/Type/Search/Video success。
- [x] B 站等动态页面必须忽略广告、banner、推荐流、热词、计数器作为成功证据。
- [x] VLM verifier 必须输出结构化 JSON；parse failure 不得伪装成功。
- [x] VLM verifier 是 isolated request，不追加或改写 `state["messages"]`。
- [x] Trace 不记录原始隐私文本、raw hint、截图内容；只记录 redacted evidence summary。
- [x] Request compaction 仍只影响 `model_client.request()` 输入，不改写 `state["messages"]`。
- [x] Edge terminal guard 仍必须优先 `finished/error`。
- [x] 未经用户明确批准，不提交、不推送；本轮执行来自用户明确 autopilot 授权。
