---
description: LangGraph roadmap 与核心执行约束；仅在 RALPLAN/Autopilot 规划阶段加载
alwaysApply: false
globs: ".trae/commands/ralplan.md,.trae/commands/autopilot.md,.trae/hooks/ralplan.py,.trae/hooks/autopilot.py,.trae/rules/ralplan.mdc,.trae/rules/autopilot.mdc,.trae/rules/graph.mdc"
---

# RALPLAN Status

```yaml
status: execution_complete
design_status: critic_approved
execution_status: completed
iteration: 1
planner_mode: null
revision_scope: null
last_planner_update: "targeted revision for LocateAnything grounding fallback, DI, state/context, hash, MLX and parser acceptance"
last_architect_review: "Architect review received; revise fallback semantics, provider DI boundary, mark-vs-description routing, schema/hash/privacy, optional MLX, and parser/test coverage."
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "LocateAnything grounding roadmap implemented with provider DI, optional MLX extra, screen-bound IntentIR grounding, trace/eval/context privacy, docs, and tests."
```

# Roadmap

## 已完成 Phase

- ✅ Phase 1-14：已建立 LangGraph `Plan -> Execute -> Reflect` 主循环、设备抽象、HITL、trace/eval、context harness、canonical ActionIR、Adapter/Validator/Repair/Safety/Executor 与 prompt/output 兼容路径。
- ✅ Phase 15：已完成 IntentIR/Grounding/Gesture/Verifier/Memory 方向基础流水线与约束沉淀；本轮仅保留有效不变量，当前方向收敛为 LocateAnything 本地 grounding provider 集成。
- ✅ Phase LAG-1~LAG-4：已完成 LocateAnything/Fake `GroundingProvider` contract、optional MLX packaging、IntentIR target routing、screen/hash-bound grounding、canonical ActionIR integration、`<box>` parser、trace/eval/context privacy hardening、文档与测试。

## RALPLAN-DR 摘要

### Principles

1. **语义、定位、执行分层**：主 VLM/Planner 表达任务意图和目标描述；LocateAnything 只做屏幕绑定 target-to-bbox；Executor 只执行 validated + safety-approved canonical ActionIR。
2. **精确 fail-closed**：target-required grounding 失败不得回退为主 VLM 直接坐标 Tap；只能 reobserve、fail-closed、Take_over、Wait，或仅在显式 `legacy_grounding_mode` 开关允许时走旧路径。
3. **现有 mark path 与 description path 并列**：`target_mark_id` 优先走现有 MarkRegistry；`target_text_hint` / `target_role` / `target_intent` 走 LocateAnything；冲突、歧义或低置信度 fail-closed。
4. **Provider DI 边界清晰**：`GroundingProvider.ground(screenshot, target, screen_binding, timeout) -> GroundingResult`；provider 从 graph config/runtime factory 注入，MLX lazy import，executor/tool schema 不暴露 provider。
5. **观测可审计且默认脱敏**：只记录 run-local `screen_id`、raw screenshot hash、provider input image hash、bbox/center、latency、failure code 与脱敏 target hints；不记录 raw screenshot。

### Decision Drivers

1. 现有主 VLM 直接承担视觉定位和动作细节会混淆语义、grounding 与执行责任，增加误点和越权风险。
2. LocateAnything 本地 MLX provider 可把定位工程化为独立能力，但必须绑定屏幕、验证 hash、接入 canonical safety pipeline。
3. 该任务核心是 context engineering：grounding 结果、失败分类、延迟、hash 与隐私约束必须进入 trace/eval/context，而不能临时绕过 executor。

### Viable Options

| Option | Pros | Cons |
|---|---|---|
| A. 主 VLM 继续直接输出坐标/命令，仅加强 prompt | 改动最小；短期回归风险低 | 安全边界模糊；target-required grounding 失败易误回退；定位不可审计 |
| B. LocateAnything 作为独立 GroundingProvider，description path 接入现有 IntentIR/ActionIR/Safety | 责任清晰；可 DI/灰度；可绑定 hash；可观测、可测试；保留 mark path | 需要新增 provider/config/state/context/parser/eval 与测试矩阵 |
| C. 全量 UI marks/overlay 后让模型选 mark | 候选可解释；可与 OCR/UIAutomator 结合 | 用户方向要求 LocateAnything description path；隐私文本与上下文体积风险更高 |

### Selected Approach

选择 **B. 独立 LocateAnything GroundingProvider + 并列 mark/description routing**。主 VLM 输出 IntentIR target hints：优先字段 `target_text_hint`，兼容别名 `target_text` 只在 adapter 内归一化；若存在 `target_mark_id` 则走现有 MarkRegistry，若存在 description hints 则调用 LocateAnything。Grounding 输出绑定 `screen_id`、raw screenshot hash、provider input image hash，经 `<box>` parser 得到 0-1000 bbox/center，再编译为 canonical Tap/LongPress 等 ActionIR，并严格经过 Validator、limited Repair、二次 Validator、Safety/HITL 与 Executor。

目标流水线：`VLM intent -> Adapter/IntentValidator -> mark path 或 LocateAnything description path -> canonical ActionIR -> Validator -> Repair -> Validator -> Safety/HITL -> Executor -> Trace/Eval`。非屏幕定位动作（Launch/Back/Home/Wait/Type）继续 canonical pipeline，不强制 LocateAnything。

### ADR

- **Decision**: 引入 `GroundingProvider` 接口与 LocateAnything-3B-4bit (MLX) optional provider，作为 description target-to-bbox 的受控能力；保留现有 MarkRegistry mark path。
- **Drivers**: 分离语义与定位；避免 target-required 失败回退成直接坐标 Tap；确保 screen/hash 绑定、DI、脱敏、灰度和测试可控。
- **Alternatives**: prompt hardening；全量 marks/overlay；直接替换主 VLM 为 GUI agent。
- **Why chosen**: 最小化架构迁移，最大化复用现有 Validator/Repair/Safety/HITL/Executor，同时把定位变成可审计的 context-harness 资产。
- **Consequences**: 需要新增 provider interface、runtime factory、MLX optional extra/lazy import、state/context schema、hash 校验、parser 与 fake/integration 测试；短期配置复杂度上升。
- **Follow-ups**: 基于 eval 决定是否加入 OCR/UIAutomator hints、bbox calibration、多目标 disambiguation 与 gesture compiler 扩展。

## 已完成 LocateAnything Grounding Phase

### Phase LAG-1 Provider Contract / Config / MLX Packaging

- **目标/根因**: 定义稳定 provider interface 与 DI 边界，封装 LocateAnything-3B-4bit (MLX) 为可选本地 provider。根因是 grounding 能力必须从 executor/tool schema 与主 VLM 输出中隔离，且默认 CI 不能依赖真实 MLX 模型。
- **修改文件候选**: `phone_agent/grounding/*`, `phone_agent/config/*`, `phone_agent/graph/state.py`, `requirements.txt`, `setup.py`, `scripts/download_locateanything.py`, `tests/model/*`, `tests/graph/*`。
- **执行步骤**:
  1. 定义 `GroundingProvider.ground(screenshot, target, screen_binding, timeout) -> GroundingResult`、错误码、timeout 与 provider registry/factory；provider 从 graph config/runtime factory 注入。
  2. 实现 fake provider 作为默认单测 provider；LocateAnything MLX provider 使用 optional extra、darwin/arm64 + 模型存在时才启用，MLX lazy import，真实模型测试标记为 integration/slow。
  3. 明确 executor/tool schema 不暴露 provider、backend 或模型参数；关闭 provider 时仅影响 target-required description grounding，不影响 Launch/Back/Home/Wait/Type canonical pipeline。
- **验收标准**:
  - 默认 `.venv/bin/pytest` 不需要 MLX/模型即可通过 fake provider 单测。
  - `requirements.txt` / `setup.py` 将 MLX/LocateAnything 依赖放入 optional extra；非 darwin/arm64 或无模型时不阻塞默认 CI。
  - provider 只能经 graph config/runtime factory 注入；executor/tool schema、Action tool 参数中看不到 provider 字段。
  - provider 缺失、disabled、timeout、MLX import/model load failure 均返回结构化 failure code，不直接执行设备动作。

### Phase LAG-2 Intent Target Routing / Context Schema

- **目标/根因**: 扩展 IntentIR 与 context，使 mark path 和 LocateAnything description path 并列建模。根因是现有 `target_mark_id` 已有 MarkRegistry 语义，不能被 description grounding 覆盖；target hints 又需要脱敏进入 context/trace。
- **修改文件候选**: `phone_agent/actions/ir.py`, `phone_agent/actions/adapter.py`, `phone_agent/actions/validator.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/observation.py`, `phone_agent/graph/context.py`, `phone_agent/model/client.py`, `tests/actions/*`, `tests/graph/*`, `tests/model/*`。
- **执行步骤**:
  1. 字段优先使用 `target_text_hint`，`target_text` 仅作为 adapter 兼容别名归一化；保留 `target_role`、`target_intent`、`target_mark_id`、`requires_grounding`。
  2. 路由规则：`target_mark_id` 优先走现有 MarkRegistry；无 mark id 但有 description hints 时走 LocateAnything；mark id 与 description 语义冲突、歧义、多候选或低置信度 fail-closed。
  3. 增加 state/context schema：`grounding_result`, `grounding_provider`, `grounding_latency_ms`, `grounding_failure_code`, `grounding_screen_hash`, `grounding_observation` context section；target hints 默认 private 并在 trace/context 中 redaction。
- **验收标准**:
  - Adapter/validator 单测覆盖 `target_text_hint`、`target_text` alias、mark-id 优先、description provider 其次、冲突/歧义 fail-closed。
  - Plan context 只注入脱敏且有界的 `grounding_observation`，不注入 raw screenshot、raw OCR/UI text 或隐私 target hint。
  - 主 VLM 输出绝对像素、backend、ADB/shell 命令、provider 选择字段时 fail-closed；parse retry 只修格式，不猜目标或坐标。
  - 非屏幕定位动作继续 canonical pipeline，且不要求 grounding 字段存在。

### Phase LAG-3 Screen-Bound Grounding / ActionIR Integration

- **目标/根因**: 将 LocateAnything provider 输出绑定当前屏幕并编译为 canonical ActionIR。根因是 grounding 只有验证 screen/hash 后进入 Validator/Safety/HITL，才可避免 stale screen、误回退和 double conversion。
- **修改文件候选**: `phone_agent/actions/grounding.py`, `phone_agent/actions/ir.py`, `phone_agent/actions/validator.py`, `phone_agent/actions/repair.py`, `phone_agent/actions/safety.py`, `phone_agent/graph/nodes/execute.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/state.py`, `phone_agent/graph/observation.py`, `phone_agent/model/client.py`, `phone_agent/graph/tools/*`, `requirements.txt`, `setup.py`, `scripts/download_locateanything.py`, 相关 `tests/actions/*`, `tests/graph/*`, `tests/model/*`, `tests/evals/*`。
- **执行步骤**:
  1. 区分并传递 run-local `screen_id`、raw screenshot hash、provider input image hash；provider request/response 必须携带并验证这些 hash，只记录 hash 不记录 raw screenshot。
  2. 调用 `GroundingProvider.ground(...)` 后将 bbox center 编译为 canonical Tap/LongPress 等 ActionIR，坐标保持 0-1000；relative-to-absolute 仍只允许 tool/backend 转换。
  3. 精确失败策略：target-required grounding 出现 stale screen/hash mismatch/unknown target/ambiguous/low confidence/bad bbox/provider failure 时，只能 reobserve、fail-closed、Take_over、Wait；仅显式 `legacy_grounding_mode` 允许旧路径，且必须可观测。
  4. 保持 Repair 只做有限格式/别名修复并二次 Validator；Safety/HITL/Executor 不接收 provider object 或 raw target text。
- **验收标准**:
  - 单测覆盖 success target-to-tap、mark path、description path、hash mismatch、stale screen、unknown/ambiguous target、provider unavailable、legacy mode gated fallback。
  - `validate_action()` 前后无 provider/raw screenshot 泄漏；Executor 只接收 validated + safety-approved canonical ActionIR。
  - tool/backend 是唯一 relative-to-absolute 转换点，无 double conversion；模型不能选择 backend/命令。
  - target-required 失败路径不会回退为主 VLM 直接坐标 Tap；非定位动作仍可执行 canonical pipeline。

### Phase LAG-4 Parser / Trace / Eval / Privacy Hardening

- **目标/根因**: 完成 `<box>` parser、trace/eval 与隐私观测闭环。根因是本地 grounding provider 需要可度量、可回归、可灰度，且 parser 与 hash/隐私契约必须可测试。
- **修改文件候选**: `phone_agent/grounding/*`, `phone_agent/graph/trace.py`, `phone_agent/graph/context.py`, `phone_agent/graph/verifier.py`, `evals/*`, `tests/evals/*`, `tests/graph/*`, `tests/actions/*`, `tests/model/*`。
- **执行步骤**:
  1. `<box>` parser 校验 bbox ordering、0-1000 range、min/max area、center deterministic、resize calibration 与 parse failure code。
  2. trace/eval 记录脱敏 metadata：provider name/version、latency、bbox/center、screen_id、raw screenshot hash、provider input image hash、failure code、redacted target summary；不记录 raw screenshot/raw target text。
  3. eval 增加定位精度、bbox sanity、失败分类、reobserve/Take_over/Wait/fail-closed 次数、provider latency 与 end-to-end success 指标。
  4. 用 golden trace/schema contract 验证 target hints 默认 private、URL/API key/token/raw OCR/UI text 不落盘。
- **验收标准**:
  - parser 单测覆盖 bbox 顺序修正或拒绝、越界、过小/过大面积、center deterministic、resize calibration、坏格式和空输出。
  - `.venv/bin/pytest tests/actions tests/graph tests/evals -v` 覆盖 trace/eval contract、失败分类、hash 验证与脱敏。
  - trace/eval 可关联每次 grounding 与最终 ActionIR/安全决策，但只保存 hash、bbox、计数、错误码和脱敏 metadata。
  - integration/slow 真实 MLX 测试仅在 darwin/arm64 且模型存在时运行；默认 CI 不阻塞。

# 约束 Checklist

- [x] RALPLAN 规划阶段已完成并获 Critic APPROVE；本轮 execution 已按用户显式 autopilot/handoff 执行。
- [x] 保持 LocateAnything LAG 方向：主 VLM 负责语义/目标描述，LocateAnything 只做 `screenshot + target description -> bbox/center`，不做规划、安全或执行决策。
- [x] target-required grounding 失败不得回退为主 VLM 直接坐标 Tap；只允许 fail-closed/Take_over/Wait/reobserve 语义，当前实现 fail-closed 为 `model_parse_failed`。
- [x] 非屏幕定位动作（Launch/Back/Home/Wait/Type）继续 canonical pipeline，不强制 grounding。
- [x] `GroundingProvider.ground(screenshot, target, screen_binding, timeout) -> GroundingResult` 是唯一 provider contract；provider 由 graph config/runtime factory/env 注入；MLX lazy import；executor/tool schema 不暴露 provider。
- [x] `target_mark_id` 优先走现有 MarkRegistry；`target_text_hint` / `target_role` / `target_intent` 走 LocateAnything/Fake provider；`target_text` 仅作为兼容别名；危险 provider/backend 字段 fail-closed。
- [x] grounding state/context 包含 `grounding_result`, `grounding_provider`, `grounding_latency_ms`, `grounding_failure_code`, `grounding_screen_hash` 与 `grounding_observation` section。
- [x] target hints 默认 private；trace/context/eval 只记录脱敏摘要、hash、bbox、计数、错误码和必要 metadata，不记录 raw screenshot、raw OCR/UI text、API key、token 或隐私 raw text。
- [x] 区分 run-local `screen_id`、raw screenshot hash、provider input image hash；provider request/response 携带并验证 hash，落盘只保存 hash。
- [x] bbox/center 坐标统一为 0-1000；tool/backend 是唯一 relative-to-absolute 转换点，防止 double conversion。
- [x] 所有 grounding 输出进入 canonical `ActionIR -> Validator -> Repair -> Validator -> Safety/HITL -> Executor`；Repair 只修有限格式/别名且二次 Validator。
- [x] `<box>` parser 校验 bbox ordering、range、min/max area、center deterministic、resize calibration。
- [x] MLX/LocateAnything 依赖是 optional extra；fake provider 覆盖默认单测；真实模型路径不会阻塞默认 CI。
- [x] Launch app 仍以现有 registry 为准；未知 app/package/action fail-closed。
- [x] 测试命令使用项目虚拟环境 `.venv/bin/pytest`；默认测试不依赖真实模型或外部服务。
