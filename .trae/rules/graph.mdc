---
description: LangGraph roadmap 与核心执行约束；仅在 RALPLAN/Autopilot 规划阶段加载
alwaysApply: false
globs: ".trae/commands/ralplan.md,.trae/commands/autopilot.md,.trae/hooks/ralplan.py,.trae/hooks/autopilot.py,.trae/rules/ralplan.mdc,.trae/rules/autopilot.mdc,.trae/rules/graph.mdc"
---

# RALPLAN Status

```yaml
status: iterate_after_review
design_status: critic_approved
execution_status: partial
iteration: 1
planner_mode: null
revision_scope: "docs synchronized to actual code state; implementation blockers remain"
last_planner_update: "Finalized approved Hybrid Accessibility Grounding Hardening roadmap after targeted revision."
last_architect_review: "Second Architect found no blockers; factory metadata carrier, sanitizer requirements, privacy scope, and malformed XML fail-closed behavior are executable."
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "Autopilot implemented the main Hybrid Accessibility Grounding Hardening pieces, but multi-agent review found remaining blockers: hint-mismatched provider marks can still enter executable marks, diagnostic code fields need strict sanitizers, hybrid wrapper does not surface child parse_summary, and accessibility failure codes need taxonomy sync."
```

# Roadmap

## 已完成 Phase

- ExpectedOutcome、Context Harness、structured JSON/tool_calls adapter、ActionIR Validator/Repair/Safety Gate、trace/checkpoint redaction、MarkRegistry grounding、AccessibilityTree hybrid provider、ScreenStructure/ObjectRegistry、object selector grounding、LocateAnything structured visual sidecar 已完成或已进入代码事实。
- 当前代码事实核对：`BOUNDS_RE` 已支持负坐标；`dump_uiautomator_xml()` 已只读取 stdout 并使用 `errors="replace"`；hybrid 默认通过 `build_mark_providers()` 生成 `AccessibilityTreeProvider -> LocateAnythingMLXProvider` 的 `FallbackMarkProvider`；plan/reflect 在 hybrid 模式下会跳过直接 accessibility base-mark 注入。
- 本次 roadmap 清理旧视觉结构计划，只保留最小历史摘要；新的规划聚焦 hybrid/accessibility grounding 的生产硬化和观测闭环，避免重复实施已完成修复。
- 最新多 agent 审查结论：signed/whitespace bounds、stdout-only XML、hybrid_factory/synthetic skip row 是真实修复；但 default hybrid 仍会把 hint-mismatched tree marks 放进最终 executable `marks`，`fallback_chain`/`hybrid_factory`/`parse_summary` 的 code 字段 sanitizer 还不是 strict schema，child provider `parse_summary` 未透传到顶层 hybrid result，新增 accessibility failure codes 还未进入 Plan grounding taxonomy。

## RALPLAN-DR 摘要

### Principles

1. **Fix only remaining risks**: 已经落地的 signed bounds、stdout-only XML 和 hybrid base-mark gating 不再重复规划；本轮只补仍可能导致真实设备错点、漏 mark 或不可诊断的边界。
2. **Tree-first, fail-closed**: Accessibility tree 仍是 hybrid 第一层；解析失败、hint 不匹配、provider 缺失或多候选歧义不能回退到主 VLM raw coordinate。
3. **Observability before fallback guessing**: hybrid factory、fallback chain、XML parse、bounds normalize、screen structure summary 必须给出脱敏的 failure code 与 evidence count，便于定位“为什么没点到”；新增诊断字段只允许 code/count/bool/hash/length，不承载 raw XML、raw hint 或 raw private text。
4. **Parser tolerance without semantic invention**: 解析器可容忍负数、空白、控制字符和部分 XML 噪声，但不得伪造 clickable/focusable/resource id 等 Android 控件真值。
5. **Regression tests reflect real device shapes**: 横竖屏、foldable/负坐标、空白 bounds、WebView/Flutter tree partial、无 dump callback、stderr XML、动态列表噪声都要用 fixture 固化。

### Decision Drivers

1. 真实设备上 UiAutomator bounds 可能包含负坐标或被 ROM/工具链格式化；当前 regex 已支持负数，但不支持内部空白，仍可能丢节点。
2. XML dump 可能包含控制字符、未转义字符、截断 hierarchy 或 stderr 噪声；当前 stdout-only 已正确，但 parser 仍是整体 `ET.fromstring()` fail-closed，缺少可诊断的 parse summary。
3. hybrid factory 在缺少 accessibility dump callback 时会直接构建 LocateAnything fallback；这是可运行的降级，但当前缺少明确 metadata/trace，用户难以知道 tree-first 为什么没发生。
4. Fallback provider 已有 hint-aware usable gate，但 failure taxonomy、candidate retained counts、structure retained counts 与 plan/reflect trace 的汇总仍需要更系统的生产级回归保护。

### Research Evidence Notes

| Source | Evidence used |
|---|---|
| Local code inspection | `phone_agent/grounding/accessibility.py` 已有 signed `BOUNDS_RE` 和 normalization；`phone_agent/adb/device.py` 已只信 stdout；tests 已覆盖 negative bounds、stderr XML、hybrid skip direct base marks。 |
| Smart Search query: `Android uiautomator dump XML bounds whitespace negative coordinates AccessibilityNodeInfo getBoundsInScreen` | 工程资料支持 bounds 来自 screen-relative rect，负数/越界在滚动、foldable、多窗口等场景可能出现；推荐 regex 同时支持 `-` 和 `\s*`。 |
| Context7 Android Developers `/websites/developer_android` | Android 文档源可用；bounds 是 screen-coordinate Rect 语义，平台字段必须来自 accessibility node，不应由视觉或 parser 伪造。 |
| Project memory: hybrid grounding review | 旧 review 指出 signed bounds、stdout/stderr 隔离、factory silent fallback、hint-aware fallback gating 是核心风险；当前前两项已修，后两项仍需观测和回归加固。 |

### Viable Options

| Option | Pros | Cons | Verdict |
|---|---|---|---|
| A. Full rewrite of grounding pipeline | 可统一清理历史接口 | 风险高，容易破坏已完成 visual/object work；与“根据已找到问题硬化”不匹配 | Reject |
| B. Minimal bug patch only | 快速修空白 bounds 等单点 | 不能解决 silent fallback、trace 不可诊断和真实设备回归覆盖不足 | Too narrow |
| C. Hardening phase: tolerant parser + provider observability + fallback taxonomy + focused tests | 聚焦已发现问题；保留现有架构；可验收；不改变执行边界 | 需要跨 adb/grounding/graph/tests/docs 小范围同步 | Selected |
| D. Benchmark-first large evaluation phase | 能量化 grounding 质量 | 在解析和观测缺口没补齐前，benchmark 失败原因仍不清楚 | Follow-up |

### Selected Approach

选择 **C. Hybrid Accessibility Grounding Hardening**。

目标运行策略：

```text
Plan/Reflect Observation
  -> Build hybrid provider with explicit tree availability metadata
  -> Dump UiAutomator XML from stdout only, with timeout/truncation/error code
  -> Parse XML tolerantly enough for real device bounds/text noise
  -> Preserve accessibility marks/structure only when screen binding is valid
  -> Run LocateAnything only when tree is unavailable or hint-unusable, unless visual screen mode is explicitly enabled
  -> Emit sanitized fallback_chain + candidate/structure counts; accessibility provider emits parse_summary, but default hybrid parse_summary propagation is still a follow-up
  -> Resolver still compiles only to unique atomic target_mark_id
```

### ADR

- **Decision**: 建立一个小而完整的 hardening phase，覆盖 accessibility XML/bounds 容错、hybrid factory 降级可观测、fallback failure taxonomy、plan/reflect trace summary、回归测试和文档同步；审查后文档先按实际代码状态标注未闭环项。
- **Drivers**: 已发现问题集中在真实设备边界和诊断盲区；核心 ActionIR/visual sidecar 架构不应重做。
- **Alternatives**: 单点修复；全量重构；先做 benchmark。
- **Why chosen**: 改动面可控，直接对应已发现问题，能提高线上问题归因质量，且不改变 executor 安全边界。
- **Consequences**: 会新增少量 failure code、metadata summary 和 fixture；默认行为应保持兼容，但 trace/report 中会出现更明确的 hybrid/accessibility 诊断字段。当前实现尚未完全兑现 default hybrid 的 parser-level 诊断闭环。
- **Follow-ups**: 先修复 executable mark 过滤、strict diagnostic sanitizer、hybrid child parse_summary 汇总和 accessibility error taxonomy；之后再考虑真实设备 benchmark、wrong-target regression dashboard、更多 ROM/device corpus。

## 当前规划 Phase: Hybrid Accessibility Grounding Hardening

### Step 1 - Bounds And XML Parser Tolerance

- **根因**: 真实设备 XML bounds 可能包含负数、内部空白、越界坐标；XML 本身可能有控制字符或局部无效字符。当前 signed bounds 已修，但 `BOUNDS_RE` 仍要求无空白，`ET.fromstring()` 出错时缺少细粒度诊断。
- **修改文件候选**: `phone_agent/grounding/accessibility.py`, `tests/actions/test_grounding_provider.py`, `tests/graph/test_objects.py`。
- **计划**:
  1. 将 bounds regex 扩展为容忍空白的 signed pattern：`\[\s*(-?\d+)\s*,\s*(-?\d+)\s*\]\s*\[\s*(-?\d+)\s*,\s*(-?\d+)\s*\]`。
  2. 保持 `_normalize_bounds()` 对越界值 clamp 到 0-1000，不改变坐标执行边界。
  3. 新增 XML pre-parse 清洗只处理非法控制字符和 BOM；不做未转义 `&`、缺失闭合标签或层级错乱的字符串修复，不伪造节点。
  4. `parse_uiautomator_marks()` 与 `parse_uiautomator_structure()` 对 parse failure 返回空/None，但同时提供内部 helper 可产出 canonical `parse_summary`：`xml_status`、`raw_node_count`、`mark_count`、`structure_node_count`、`bounds_parse_fail_count`、`filtered_zero_area_count`、`interactive_candidate_count`。`raw_node_count` 仅表示解析后、mark 过滤前的 XML node 数；若 XML 未能解析，计数为 0 或省略，但必须有 `xml_status=accessibility_xml_parse_error`。
- **验收标准**:
  - fixture 覆盖 `bounds="[ -10, 20 ][ 1080, 2400 ]"`、负数、越界、空白、无效 bounds、零面积 bounds。
  - XML 控制字符不会导致整个 provider 未捕获异常；未转义 ampersand、截断 hierarchy、缺失闭合标签按 `accessibility_xml_parse_error` fail-closed 记录，不做 repair。
  - 不新增 raw text 泄漏；summary 只含计数和 failure code。

### Step 2 - Accessibility Provider Parse Summary

- **根因**: 当前 provider 失败时只看到 no marks 或 provider error，无法判断是 dump 空、XML 截断、parse error、bounds 全失败、还是节点均不可用。
- **修改文件候选**: `phone_agent/grounding/accessibility.py`, `phone_agent/grounding/provider.py`, `phone_agent/graph/observation.py`, `tests/actions/test_grounding_provider.py`, `tests/graph/test_state.py`。
- **计划**:
  1. `AccessibilityTreeProvider` metadata 增加脱敏 `parse_summary`：`xml_status`、`raw_node_count`、`mark_count`、`structure_node_count`、`bounds_parse_fail_count`、`filtered_zero_area_count`、`interactive_candidate_count`。
  2. failure code 细分为 `accessibility_dump_empty`、`accessibility_xml_parse_error`、`accessibility_no_interactive_marks`、`accessibility_structure_missing`。
  3. Observation 的 `mark_provider_observation.providers[].metadata` 保留 bounded parse summary，不保留 XML、raw text、resource id 原文。
  4. parse summary 不影响 Safety/Executor，只用于 trace、diagnosis 和 fallback gate。
  5. 隐私范围明确：本 phase 只约束新加的 `parse_summary`、`fallback_chain`、`hybrid_factory`、compact observation summary 必须 code/count/bool/hash/length-only；现有 `MarkCandidate.text_summary`、`MarkRegistry.prompt_block()` 和 Screen Objects 的 prompt-safe 文本契约保持不变，除非另起 phase。
- **验收标准**:
  - plan/reflect trace 可区分“dump 成功但无可点击节点”和“XML parse 失败”。
  - 新增 diagnostic metadata 通过 `sanitize_context_payload` 或同等约束，不出现手机号、邮箱、验证码、raw XML、raw hint、raw resource id、raw OCR/private text。
  - 测试明确证明新增 diagnostic metadata raw-text-free；同时证明现有 prompt-safe `text_summary` 行为不被本 phase 回归。
  - 旧调用方不读 metadata 时行为不变。

### Step 3 - Hybrid Factory Degradation Visibility

- **根因**: `build_mark_providers()` 在 hybrid 模式缺少 `accessibility_tree_dump` 时仍会构建 LocateAnything fallback，这是合理降级，但当前不够显式，容易误以为 tree-first 已执行。
- **修改文件候选**: `phone_agent/grounding/factory.py`, `phone_agent/grounding/fallback.py`, `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/nodes/reflect.py`, `tests/actions/test_grounding_provider.py`, `tests/graph/test_plan_reflect.py`。
- **计划**:
  1. 选择明确 carrier：给 `FallbackMarkProvider` 增加可选 `composition_metadata: dict[str, Any]` init 参数；factory 在 hybrid 分支构造该 metadata。
  2. `FallbackMarkProvider.provide_marks()` 在每个返回的 `MarkProviderResult.metadata` 中合并 `hybrid_factory`，schema 固定为 `hybrid_mode: true`、`accessibility_child_enabled: bool`、`accessibility_child_skip_reason: null|accessibility_dump_callback_missing|skip_accessibility_provider`、`provider_order: list[str]`。
  3. 同时在 `fallback_chain` 最前面追加一个 synthetic skip row 仅当 accessibility child 未构建：`provider=accessibility_tree`、`success=false`、`mark_count=0`、`candidate_count=0`、`structure_count=0`、`usable=false`、`skip_reason=<reason>`、`failure_code=<reason>`、`latency_ms=0`；这样 observation/trace 不需要额外路径也能解释 tree layer 未运行。
  4. `phone_agent/graph/observation.py::_safe_fallback_chain()` 必须显式保留并 sanitize `structure_count`、`skip_reason`；`hybrid_factory` summary 只保留上述 bounded schema。
  5. 如果 explicit config 请求 hybrid 且 dump callback 缺失，保留 LA fallback 但在 `hybrid_factory` 和 synthetic fallback row 标记 `accessibility_dump_callback_missing`；不得静默伪装成完整 hybrid。
  6. plan/reflect 注入 dump callback 的路径保持现有 DeviceFactory DI，不把 `device_factory` 暴露到 tool schema。
  7. direct `build_mark_provider()` 与 plural `build_mark_providers()` 的行为在 README/代码注释中区分：默认 graph path 使用 plural，single provider path 不应承诺 hybrid composition。
- **验收标准**:
  - fixture 覆盖 hybrid + missing dump callback，最终 provider 可用，`metadata.hybrid_factory.accessibility_child_skip_reason == "accessibility_dump_callback_missing"`，且 `fallback_chain[0].skip_reason` 同步可见。
  - fixture 覆盖 hybrid + `skip_accessibility_provider=True`，skip reason 与 base-mark 去重原因可观测。
  - `_safe_fallback_chain()` 输出保留 sanitized `structure_count` 与 `skip_reason`，并拒绝/裁剪非 schema 字段。
  - 不改变默认 `AgentConfig.grounding_provider_name == "hybrid"`。

### Step 4 - Fallback Chain Failure Taxonomy

- **根因**: Fallback 已 hint-aware，但 provider 成功、marks miss hint、candidates retained、structure retained、screen-mode supplemental 的语义还需要稳定成可测试 contract。
- **修改文件候选**: `phone_agent/grounding/fallback.py`, `phone_agent/graph/observation.py`, `tests/actions/test_grounding_provider.py`, `tests/graph/test_state.py`。
- **计划**:
  1. `fallback_chain` 每项固定包含 `provider`、`success`、`failure_code`、`mark_count`、`candidate_count`、`structure_count`、`usable`、`skip_reason`、`latency_ms`。
  2. 当所有 provider 都有 marks 但均不匹配 hint，统一 status/failure code 为 `grounding_no_usable_candidate`，保留 candidates 与 structures 供诊断，但 `marks=[]`。
  3. 当某个 provider marks 不匹配 hint，不能把这些 marks 追加到最终 executable `marks`；只能保留到 diagnostic candidates/structures，除非该 provider 自身 `usable=True`。当前代码仍需修复这一点。
  4. 当 accessibility usable 且后续 LA 不是 `screen` supplemental mode，必须停止，不调用 LA；当 LA 是显式 `screen` mode，允许只补 sidecar/candidates，不追加 executable marks。
  5. `_result_is_usable()` 的 hint term 提取继续使用 redacted/safe terms，避免 raw private hint 进入 metadata。
  6. `_safe_fallback_chain()` 是 trace/checkpoint/prompt 之前的 sanitizer gate；只允许 bounded fields，不透传 provider 原始 message、hint、XML 或 candidates。provider、failure_code、xml_status、provider_order 等 code 字段必须使用 enum/safe-identifier allowlist，不能只做 regex redaction。
- **验收标准**:
  - 现有 fallback tests 继续通过，并新增 structure_count/skip_reason 断言。
  - wrong-hint 情况 fail-closed，不回退 raw bbox tap，且不匹配 hint 的 provider marks 不进入 final executable marks。
  - screen supplemental mode 不污染 executable mark list。
  - sanitizer 单测覆盖 `fallback_chain.structure_count`、`fallback_chain.skip_reason`、恶意 extra raw fields 被丢弃，并覆盖恶意文本塞进 allowed code fields 时被归一为 `unknown`/受控 code。

### Step 5 - Plan/Reflect Trace And Error Path Integration

- **根因**: 线上排查需要知道 observation 生成了哪些 marks/objects/structures，以及失败发生在 dump、parse、fallback gate、adapter、validator 还是 resolver。
- **修改文件候选**: `phone_agent/graph/nodes/plan.py`, `phone_agent/graph/nodes/reflect.py`, `phone_agent/graph/trace.py`, `phone_agent/graph/observation.py`, `tests/graph/test_plan_reflect.py`。
- **计划**:
  1. `plan_start`、`plan_result`、`plan_error`、`reflect` 相关 trace 都带 `mark_provider_observation` 的 compact summary。
  2. summary 包含 provider_count、provider_order、fallback_chain、hybrid_factory、screen_structure_summary、object_registry counts、failure_code，不包含 screenshot、XML、raw OCR、raw hint。
  3. default hybrid result 需要把 accessibility child 的 safe `parse_summary` 汇总到 wrapper metadata，或在文档和 trace schema 中明确 provider-level 与 wrapper-level 的区别；当前实现尚未汇总。
  4. parse/model error 路径也保留 observation summary，避免模型失败后丢失 grounding 诊断。
  5. Reflect after-observation 默认仍不触发 LocateAnything fallback；只记录 accessibility/device marks unless explicit `reflect_enable_vlm_grounding`。
- **验收标准**:
  - 单测覆盖 `plan_error` 仍包含 provider/fallback summary。
  - 单测覆盖 `hybrid_factory` 从 provider result metadata 到 observation/trace 的完整路径。
  - 单测覆盖 default hybrid 下 child `parse_summary` 可被诊断到，或显式验证文档声明的 provider/wrapper 分层。
  - confirm/takeover/HITL 路由不受 summary 字段影响。
  - trace JSON 可关联 `.traces/{trace_id}.jsonl`，且敏感字段脱敏。

### Step 6 - Focused Regression Suite

- **根因**: 已发现问题多来自真实设备边界，必须用无需 MLX/Metal 的 fixture 固化。
- **修改文件候选**: `tests/actions/test_grounding_provider.py`, `tests/graph/test_plan_reflect.py`, `tests/graph/test_state.py`, `tests/graph/test_objects.py`, `tests/test_adb_device_signals.py`。
- **计划**:
  1. 增加 bounds fixture：负坐标、内部空白、越界、foldable-like、横屏、零面积。
  2. 增加 XML fixture：stderr XML ignored、stdout truncated、control char cleaned、invalid ampersand fail-closed as `accessibility_xml_parse_error`、empty hierarchy。
  3. 增加 hybrid fixture：missing dump callback、skip accessibility child、tree marks miss hint then LA usable、tree usable then LA not called、screen supplemental LA called but not executable。
  4. 增加 trace fixture：plan_result/plan_error/reflection 均有 compact observation summary。
  5. 增加 sanitizer fixture：`parse_summary`、`fallback_chain.structure_count`、`fallback_chain.skip_reason`、`hybrid_factory` 保留 schema 字段并丢弃 raw XML/raw hint/raw private text。
- **验收标准**:
  - 默认单测不依赖真实 Android、ADB、MLX、Metal。
  - 推荐验证命令：`.venv/bin/python -m pytest tests/actions/test_grounding_provider.py tests/graph/test_plan_reflect.py tests/graph/test_state.py tests/graph/test_objects.py tests/test_adb_device_signals.py`。
  - 若完整 suite 太慢，至少跑以上 focused suite；Autopilot 执行阶段再决定是否扩大。

### Step 7 - Documentation And Roadmap Sync

- **根因**: hybrid/accessibility 的默认行为、降级行为和诊断字段属于一线排查入口，必须同步文档，否则后续会误判 fallback 是否工作。
- **修改文件候选**: `README.md`, `docs/future-roadmap.md`, `.trae/rules/graph.mdc`, `.trae/rules/tools.mdc`, `AGENTS.md`（仅仓库内存在时）。
- **计划**:
  1. README grounding/env section 说明默认 `hybrid`、tree dump callback、missing callback 降级、fail-closed 和 trace 字段。
  2. `docs/future-roadmap.md` 标记本 phase 为 Hybrid Accessibility Hardening，区分已完成 visual sidecar 与新 hardening。
  3. `.trae/rules/tools.mdc` 若仍包含旧 sandbox escalation wording，按当前 TraeCLI 权限模型同步，但不在 RALPLAN 阶段改。
  4. 文档中明确 `bench_output/` 默认不提交，真实 LA benchmark 依赖 Metal，不把环境失败当模型失败。
- **验收标准**:
  - 文档能回答“tree-first 为什么没有跑”“为什么 fallback 到 LA”“为什么没有点击到候选”“trace 里看哪个字段”。
  - 不提交临时 evidence、bench output、runtime traces。

## 约束 Checklist

- [x] Planner 只修改 `.trae/rules/graph.mdc`；业务代码修改必须等待 Critic APPROVE 和用户明确批准。
- [x] Planner initial rewrite 已清理旧 roadmap，旧视觉结构计划仅保留已完成摘要。
- [x] `target_mark_id` 仍是唯一执行边界；任何 selector 成功后也必须编译为唯一 atomic mark。
- [x] `target_text_hint` 只能作为 provider hint，不能成为可执行目标。
- [x] 多候选、低置信、stale/hash mismatch、无 sidecar、hint mismatch、结构歧义必须 fail-closed。
- [x] Raw hint、raw OCR text、raw XML、raw private text、截图不得进入 trace/checkpoint/report/prompt。
- [x] 本 phase 的新增 diagnostic metadata 只允许 code/count/bool/hash/length；现有 prompt-safe mark/object text summary 契约保持不变。
- [x] bounds parser 可容忍真实设备格式，但不得伪造控件语义。
- [x] malformed XML 不做语义修复；未转义 ampersand/截断 hierarchy 只记录 parse error 并 fail-closed。
- [x] hybrid factory 的 accessibility missing/skip 必须可观测，不得静默伪装成完整 tree-first。
- [x] hybrid factory metadata carrier 固定为 `FallbackMarkProvider(composition_metadata=...)` -> `MarkProviderResult.metadata.hybrid_factory` + synthetic fallback skip row。
- [x] plan/reflect trace 必须在 success/error 路径保留 compact observation summary。
- [x] Reflect 默认仍不触发 LA fallback；如要使用 VLM grounding，必须显式 opt-in。
- [x] 默认 test suite 不依赖真实 Android、ADB、MLX、Metal。
- [ ] 不匹配 hint 的 provider marks 只能进入 diagnostic candidates/structures，不能进入最终 executable `marks`。
- [ ] Diagnostic code 字段使用 enum/safe-identifier allowlist，禁止 raw UI/private text 通过 allowed fields 落入 trace/checkpoint/report。
- [ ] Default hybrid wrapper 透传或汇总 child provider 的 safe `parse_summary`。
- [ ] 新增 accessibility failure codes 同步进入 Plan grounding taxonomy，保持 `error_layer=grounding` 一致。
