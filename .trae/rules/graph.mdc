---
description: LangGraph roadmap 与核心执行约束；仅在 RALPLAN/Autopilot 规划阶段加载
alwaysApply: false
globs: ".trae/commands/ralplan.md,.trae/commands/autopilot.md,.trae/hooks/ralplan.py,.trae/hooks/autopilot.py,.trae/rules/ralplan.mdc,.trae/rules/autopilot.mdc,.trae/rules/graph.mdc"
---

# RALPLAN Status

```yaml
status: completed
design_status: critic_approved
execution_status: completed
iteration: 1
planner_mode: null
revision_scope: null
last_planner_update: "Autopilot execution: implemented Android UiAutomator accessibility marks, hybrid accessibility-to-LocateAnything fallback, bounded LocateAnything context, CLI/config docs, and targeted tests."
last_architect_review: "APPROVE after second-pass fixes: hybrid fallback is hint-aware, base accessibility marks no longer bypass hybrid gating, fallback_chain is observable, XML extraction is hardened, and docs are aligned."
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "Autopilot execution and multi-agent review completed; full test suite passed; no commit created."
```

# Roadmap

## 已完成 Phase

- 基础执行框架已存在并保持不变：LangGraph `Plan -> Execute -> Reflect`、DeviceFactory/ADB、HITL confirm/takeover、local JSONL trace、eval dry-run、Context Harness、request-only compaction、structured reflection、ActionIR/Validator/Repair/Safety/Executor。
- 当前 grounding 安全边界保持不变：主 VLM 的屏幕目标点击类动作只输出 `target_mark_id`；`target_text_hint` 只能作为 MarkProvider hint，不能直接生成 ActionIR；unknown mark、stale/hash mismatch、低置信、bad bbox、多候选歧义等仍 fail-closed，不回退 raw coordinate Tap。
- LocateAnything 本地 MarkProvider 已存在：MLX lazy load、`generation_mode="hybrid"`、0-1000 bbox parser、`max_size=960` 默认、provider result trace-safe summary、benchmark 入口在 `bench/grounding/`。
- 本次 Autopilot 已完成核心实现：Android UiAutomator XML parser、AccessibilityTreeProvider、ADB `dump_uiautomator_xml()` / `get_screen_marks()`、DeviceFactory 透传、Plan 阶段 accessibility marks opt-in、`hybrid` provider fallback、LocateAnything bounded context、CLI 参数/env 配置、README/docs/AGENTS/.trae 配置更新。
- 本次已完成验证：目标测试通过，扩展相关测试通过，全量 `.venv/bin/pytest tests -q` 结果 `320 passed`。

## RALPLAN-DR 摘要

### Principles

1. **Tree first, vision fallback**: Android Accessibility/UIAutomator tree 是低延迟结构化候选来源；LocateAnything 是 tree 缺失、为空或无候选时的视觉兜底。
2. **Providers only generate marks**: Accessibility tree、LocateAnything、Fake、OCR、SoM 都只能产出 screen-bound marks；可执行动作仍必须走 `target_mark_id -> MarkRegistry -> ActionIR`。
3. **Fail-closed stays mandatory**: target-required grounding 失败不得回退到主 VLM 坐标 Tap；只能 fail-closed、等待、重新观测、接管或在受控 provider 链中 fallback。
4. **LocateAnything prompt remains strict**: 使用 `apply_chat_template(..., num_images=1)` 作为正式格式入口；默认短 instruction，不注入长任务历史、reflection 或完整控件树。
5. **Context is bounded and off by default**: `locateanything_context_max_chars=0` 默认关闭额外 context；开启时仅追加短 `Context:`，服务小模型定位，不承担规划。

### Decision Drivers

1. Android UiAutomator / AccessibilityNodeInfo 提供 text、content-desc、resource-id、class、clickable/focusable/enabled 和 bounds，能低成本生成精确 screen marks。
2. LocateAnything 擅长视觉 GUI grounding，但每步调用成本高；对标准 Android 控件应避免无条件调用。
3. 现有 `build_observation()` / `MarkRegistry` / `ground_intent_to_action()` 已经是合适的 provider 合流点，改动主图拓扑没有必要。

### Viable Options

| Option | Pros | Cons |
|---|---|---|
| A. LocateAnything-only | 视觉覆盖强；沿用已有 provider | 标准控件不利用原生 bounds；延迟高；小模型承担过多定位压力 |
| B. Accessibility tree-only | 快、精确、可解释；不需要 MLX | WebView/Flutter/Canvas/自绘 UI 覆盖不全；tree 空时无法定位 |
| C. Hybrid tree-first fallback | 平均延迟低；覆盖更完整；复用 MarkRegistry 安全边界 | 需要 XML parser、fallback provider、配置和测试矩阵 |

### Selected Approach

选择 **C. Hybrid tree-first fallback**。

运行策略：

```text
Plan captures screenshot/current_app
  -> build_observation()
     -> optional accessibility base marks when PHONE_AGENT_ACCESSIBILITY_MARKS=true
     -> optional MarkProvider chain when PHONE_AGENT_GROUNDING_PROVIDER=hybrid
        -> AccessibilityTreeProvider via adb exec-out uiautomator dump /dev/tty
        -> if no usable marks, LocateAnythingMLXProvider
  -> MarkRegistry prompt block
  -> main VLM outputs target_mark_id
  -> ground_intent_to_action validates binding and emits ActionIR
  -> execute_node dispatches validated action
```

### ADR

- **Decision**: 新增 Android UiAutomator accessibility mark path，并支持 `PHONE_AGENT_GROUNDING_PROVIDER=hybrid` 的 ordered fallback provider；LocateAnything prompt 使用官方 chat template，额外 context 通过 bounded config 显式开启。
- **Drivers**: 降低标准 Android UI grounding 延迟；提高 bounds 可解释性；保持视觉 fallback 覆盖自绘/不完整 tree；不削弱 HITL、安全门和 MarkRegistry 绑定校验。
- **Alternatives**: LocateAnything-only；tree-only；完整 XML 直接塞给主 VLM。
- **Why chosen**: 最小侵入、符合现有 provider contract、可测试、可灰度。
- **Consequences**: 需要在真实设备上进一步评估 tree 命中率、fallback 触发率、LocateAnything latency 和 clickability 指标。
- **Follow-ups**: 后续可引入更细的 tree quality gate、按 hint 匹配的 ranking、真实 AccessibilityService provider，以及 hybrid benchmark 分层。

## 当前执行 Phase

### Phase H1 - Accessibility Tree Provider

- **目标/根因**: 用 Android UiAutomator tree 生成 MarkRegistry marks。根因是当前 ADB 层没有 hierarchy dump/parser，`plan_node` 只预留 `get_screen_marks()` 但无实现。
- **已修改文件**: `phone_agent/grounding/accessibility.py`, `phone_agent/adb/device.py`, `phone_agent/adb/__init__.py`, `phone_agent/device_factory.py`, `phone_agent/graph/nodes/plan.py`, `tests/actions/test_grounding_provider.py`。
- **验收标准**:
  - XML `bounds="[x1,y1][x2,y2]"` 转换为 0-1000 bbox/center。
  - 只保留 enabled/visible 且 clickable/focusable/checkable/long-clickable/scrollable 或带文本的常见交互类节点。
  - provider result 包含 `provider_input_hash`、screen binding、candidate_count 和 redacted hint summary。
  - `PHONE_AGENT_ACCESSIBILITY_MARKS=true` 或 config `accessibility_marks=True` 可把 tree marks 作为 base marks 注入。

### Phase H2 - Hybrid Fallback Provider

- **目标/根因**: 避免 tree 和 LocateAnything 无条件同时运行。根因是 `build_observation()` 会遍历所有 providers，单纯返回两个 provider 会浪费小模型调用。
- **已修改文件**: `phone_agent/grounding/fallback.py`, `phone_agent/grounding/factory.py`, `phone_agent/grounding/__init__.py`, `phone_agent/graph/nodes/plan.py`, `tests/actions/test_grounding_provider.py`。
- **验收标准**:
  - `PHONE_AGENT_GROUNDING_PROVIDER=hybrid` 构建 `FallbackMarkProvider([accessibility_tree, locateanything_mlx])`。
  - 第一 provider 成功、有 marks 且与 hint 弱匹配时停止，不调用 LocateAnything。
  - tree failure/no marks 或 tree marks 与 hint 不匹配时继续 fallback 到 LocateAnything，并合并候选。
  - provider 链仍只输出 marks，不输出 ActionIR。

### Phase H3 - LocateAnything Prompt And Context

- **目标/根因**: 小模型 prompt 必须严格、短、可配置。根因是 LocateAnything 是 grounding 小模型，不应接收主 VLM 式长上下文。
- **已修改文件**: `phone_agent/grounding/locateanything.py`, `phone_agent/grounding/factory.py`, `main.py`, `phone_agent/agent.py`, `tests/actions/test_grounding_provider.py`。
- **验收标准**:
  - 默认 instruction 仍为 `Locate the region that matches the following description: ...`。
  - 正式路径使用 `apply_chat_template(..., num_images=1)`。
  - `locateanything_context_max_chars=0` 默认不追加 context。
  - 配置开启时只追加 bounded 单行 `Context:`，不注入 XML、历史消息或 reflection。

### Phase H4 - Documentation And Trae Config

- **目标/根因**: 架构/API/配置变更必须同步用户文档和 TraeCLI 配置。根因是新 provider/env/CLI 参数若只存在代码中，后续 agent 不会稳定遵守。
- **已修改文件**: `README.md`, `docs/future-roadmap.md`, `AGENTS.md`, `.trae/traecli.yaml`, `.trae/rules/graph.mdc`。
- **验收标准**:
  - README 说明 `hybrid`、`PHONE_AGENT_ACCESSIBILITY_MARKS`、`PHONE_AGENT_ACCESSIBILITY_*`、`PHONE_AGENT_LOCATEANYTHING_CONTEXT_MAX_CHARS`。
  - TraeCLI prompt 说明 tree-first fallback 与 LocateAnything strict prompt contract。
  - future-roadmap 标记 Accessibility Tree + LocateAnything Hybrid Grounding 已完成 MVP，并把完整 structured screen state 降为未来方向。

### Phase H5 - Verification / QA

- **目标/根因**: 固化新 provider contract，避免 prompt 格式、fallback 语义或隐私边界漂移。
- **已运行测试**:
  - `.venv/bin/pytest tests/actions/test_grounding_provider.py tests/graph/test_state.py tests/graph/test_plan_reflect.py -q` -> `95 passed`。
- **待补充/后续建议**:
  - 在连接真实 Android 设备时运行 `PHONE_AGENT_GROUNDING_PROVIDER=hybrid .venv/bin/python main.py --output-mode json_schema "打开设置"` smoke，记录 accessibility mark count、fallback 是否触发、latency。
  - 后续 benchmark 增加 hybrid suite：tree-only、LocateAnything-only、hybrid 的 center-hit/IoU/latency 对比。

# 约束 Checklist

- [x] 坐标仍统一为 0-1000，相对坐标到绝对像素转换仍只在 tool/backend 层。
- [x] Tap/Double Tap/Long Press structured screen targeting 仍必须使用 `target_mark_id`。
- [x] Accessibility tree / LocateAnything provider 不暴露给 tool schema，不直接执行设备动作。
- [x] Provider raw hint 不进入 trace/checkpoint/prompt/report；provider summaries 只记录 bounded/redacted metadata。
- [x] LocateAnything prompt 使用官方 chat template 路径；默认不注入额外 context。
- [x] Hybrid fallback 不削弱 MarkRegistry stale/hash/confidence 校验或 Safety/HITL。
- [x] 文档已同步 README、future-roadmap、AGENTS 与 `.trae/traecli.yaml`。
- [x] 多轮 code-review / architect / security-review 已完成，ITERATE findings 已修复，最终 focused code-review APPROVE。
- [x] 全量测试 `.venv/bin/pytest tests -q` 已通过，结果 `320 passed`。
- [x] 不主动 commit；禁止 force push `main` / `feature/langgraph-refactor`。
