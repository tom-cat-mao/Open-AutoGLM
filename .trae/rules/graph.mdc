---
description: LangGraph roadmap 与核心执行约束；仅在修改 graph/Agent loop/Tool/HITL 或执行 roadmap 时加载
alwaysApply: false
globs: "phone_agent/graph/**/*.py,phone_agent/agent.py,phone_agent/actions/**/*.py,phone_agent/device_factory.py,phone_agent/adb/**/*.py,tests/**/*.py,setup.py,README.md,docs/**/*.md"
---

# Design Loop Status

```yaml
status: completed
design_status: critic_approved
execution_status: completed
iteration: 3
planner_mode: null
revision_scope: null
last_planner_update: "按 Critic ITERATE 窄修：Phase 6 补充 .gitignore/tests tracking 门禁，确保 tests/**/*.py 可被 git 跟踪；补充 Tool DI schema 隐藏约束，device_factory 不得暴露到 @tool 模型可见 schema。"
last_architect_review: "Iteration 3: 窄修项已完成；Phase 6 包含 .gitignore/tests tracking 验收；Tool DI schema 隐藏约束已补充；无 Critical/Major。"
last_critic_verdict: APPROVE
approved_for_execution: true
notes: "Iteration 3 APPROVED。Phase 6 已完成：打包依赖、tests tracking、Tool DI seam、回归测试与文档事实同步均已落地并通过本地门禁。"
```

# Roadmap

## 已完成 Phase

| Phase | 结论 | 不变量迁移位置 |
|---|---|---|
| 1 | Plan-Execute-Reflect MVP 已迁移到 LangGraph StateGraph | 见下方「约束 Checklist」State/Graph loop/context 条目 |
| 2 | HITL 已使用 `interrupt()` 接入 confirm/takeover；当前只保证路由语义，不承诺持久 resume | 见 HITL、pending_execute、checkpoint 条目 |
| 3 | Action 已封装为 graph tools，`dispatch_tool()` 统一调度，坐标转换集中在 tool 层 | 见 Tool DI、坐标转换条目 |
| 4 | 旧 while loop / ActionHandler 类 / use_graph / use_tools 双轨已清理 | 见禁止恢复旧双轨条目 |
| 5 | 已修复 messages reducer、confirm-then-execute、reflect i18n、mypy clean 等问题 | 见 messages reducer、confirm-then-execute 条目 |
| 6 | 已恢复测试/打包/文档可信地基；Tool DI seam 已通过 config 注入且 schema 隐藏 `device_factory` | 见 Phase 6 门禁、依赖注入、Tool DI 条目 |

> 已完成 Phase 不再在本文件展开实现细节；执行约束统一维护在文末 Checklist，当前未完成计划从 Phase 7 开始。

## RALPLAN-DR 摘要

### Principles

1. **验证地基优先**：任何能力升级前，先修复测试、打包、依赖注入 seam 与文档一致性，避免在不可验证的地基上继续堆功能。
2. **最小可维护变更**：优先保留现有 `setup.py`，把缺失 runtime deps 补齐到唯一安装入口；不为了打包重构而引入 pyproject/setup 双源分歧。
3. **Benchmark 驱动能力增长**：对齐真实 app、长任务、多 app、步数、耗时、鲁棒性、安全 HITL、trace、UI grounding 等可度量指标。
4. **先契约再评测，先 checkpoint 再 grounding**：先定义结构化运行结果与可恢复边界，再做评测、trace、reflection、checkpoint/resume，最后增强 UI grounding。
5. **边界清晰、延迟高风险能力**：Dreaming、多 Agent、Command 动态路由、复杂 Prompt 外部化、iOS 主线、早期技能记忆暂不做。

### Decision Drivers

1. **当前验证事实不可信**：`docs/future-roadmap.md` 声称 89/89 passing，但当前源码侧 `tests/graph/` 只有 pycache 残留，缺少可执行 graph 测试；README 拓扑还把 confirm 后路由写成 `[reflect|end]`，与 `builder.py` 中 confirm → `after_interrupt` → `[execute|reflect|end]` 不一致。
2. **打包入口不闭合**：`setup.py` 已存在且 `requirements.txt` 包含 `langgraph`、`langchain-core`、`requests`，但 `setup.py install_requires` 只列出 `Pillow` 与 `openai`；fresh install 后可能无法 import graph。
3. **测试 seam 受全局单例污染**：多个 tool 当前在函数体内直接 `get_device_factory()`，fake-device 测试只能依赖 monkeypatch 全局单例；这会阻碍 eval、trace、checkpoint 等后续可重复测试。

### Viable Options

| 方案 | 优点 | 缺点 |
|---|---|---|
| A. 直接做 LangFuse / eval / grounding，测试和依赖稍后补 | 短期演示更快 | 当前安装、README、测试、Tool DI 均不可信，后续问题难定位，Critic 已判定需 ITERATE |
| B. 保留 `setup.py` 做最小打包修复 + 补 graph 测试 + Tool DI seam，再推进 structured eval/trace/checkpoint/grounding | 变更小，立刻修复 fresh install 与 fake 测试；每个后续能力有可回归证据 | 前期偏基础设施，功能展示不如直接做新能力显眼 |
| C. 迁移到 `pyproject.toml` 并重建打包体系 | 更现代，长期可维护 | 当前不是必要条件；易产生 setup/pyproject 双源冲突；会扩大 Phase 6 范围 |

### Selected Approach

选择 **方案 B：验证地基与 Tool DI seam → Structured Evaluation Harness → Observability → 策略级 Reflection → Minimal checkpoint/resume → Accessibility/OCR/UI grounding**。

理由：这条路线用最小变更先修复可安装、可测试、可注入、可文档验证的地基；再用结构化 API 和 Evaluation Harness 量化成功率、步数、耗时、HITL interrupt 触发；随后 trace 解释失败原因，reflection 改善重试策略；最后先落地 minimal checkpoint/resume，明确持久恢复边界，再做 UI grounding。跨会话 memory/技能复用仍后置到 trace、checkpoint 与足够成功轨迹之后，不进入当前 6 个执行 Phase。

## 当前规划 Phase

### Phase 7: Structured API 与 Evaluation Harness — 真实任务评测闭环

**目标**：在保持 `PhoneAgent.run() -> str` 兼容的前提下，新增结构化运行结果 API，建立面向真实 Android app 任务的评测框架，量化任务成功率、平均步数、平均耗时、错误类型与 HITL interrupt 触发情况。

**根因**：当前 `PhoneAgent.run()` 只返回字符串，eval/trace 无法稳定获取 success、finished、steps、duration、error、HITL count 等指标；外部 AndroidWorld/OSWorld/MobileWorld/AndroidDaily 类基准强调可验证任务完成，而项目当前无法回答“成功率多少、平均几步、失败在哪里”。

**修改文件候选**：
- `phone_agent/agent.py`
- `phone_agent/graph/state.py`
- `evals/tasks.json`（执行阶段可创建）
- `evals/run_eval.py`（执行阶段可创建）
- `tests/evals/test_eval_harness.py`
- `tests/graph/test_agent_run_result.py`
- `README.md`

**执行步骤与验收标准**：
1. **RunResult 契约定义** — 新增 `RunResult` dataclass/TypedDict 或等价结构，字段至少包含 `success`、`finished`、`steps`、`duration`、`final_message`、`error`、`hitl_count`、`trace_id`。验收：类型测试或单元测试能构造并断言字段默认值与序列化输出。
2. **兼容 API 暴露** — 保持 `PhoneAgent.run(task) -> str` 兼容，新增 `run_structured(task) -> RunResult` 或 `run(task, structured=True)` 等价参数供 eval/trace 使用。验收：旧调用返回字符串不变；新调用返回结构化对象/字典，错误路径也有 `error` 与 `finished` 语义。
3. **Eval harness smoke** — 提供 fake-device/fake-model/dry-run 模式，可运行小型本地 smoke benchmark 并输出 JSON 结果。验收：命令输出每条任务的 task id、task 文本、category、expected app、max_steps、success、steps、duration、final_message、error、hitl_count、trace_id。
4. **HITL 评测边界** — Phase 7 只统计 confirm/takeover interrupt 路由与 `hitl_count`，不承诺完整持久 resume；完整 resume 成功率等指标等 Phase 10 checkpoint/resume 后再加入。验收：HITL fake 任务能触发 interrupt 计数，文档明确“当前 eval 覆盖 interrupt routing，不覆盖持久恢复”。

**风险/边界**：
- 不追求立刻复刻 AndroidWorld 完整规模；先做项目内可运行、可扩展的 mini harness。
- 不把主观模型自评当作唯一成功标准；允许人工/规则/状态检查器逐步增强。
- 不在本 Phase 引入 memory 或策略优化，避免评测目标被实现复杂度污染。

### Phase 8: Observability — 本地 Trace 优先，LangFuse 可选

**目标**：让每一步 screenshot、prompt、action、result、reflection、HITL interrupt 与评测指标可追踪、可回放、可脱敏导出。

**根因**：没有 trace 时，长任务失败难以定位；真实手机 GUI Agent 涉及隐私/支付/账号操作，必须能解释每一步为什么发生；同时 Evaluation Harness 需要把失败样本关联到具体 graph step。

**修改文件候选**：
- `phone_agent/agent.py`
- `phone_agent/graph/nodes/plan.py`
- `phone_agent/graph/nodes/execute.py`
- `phone_agent/graph/nodes/reflect.py`
- `phone_agent/graph/nodes/confirm.py`
- `phone_agent/graph/nodes/takeover.py`
- `phone_agent/graph/state.py`
- `phone_agent/config/` 下新增或扩展 trace 配置（执行阶段决定具体文件）
- `tests/graph/test_trace.py`
- `tests/evals/test_trace_export.py`
- `README.md`

**执行步骤与验收标准**：
1. **本地 JSONL trace** — 默认支持无外部服务的本地 JSONL trace，记录 run id / trace id / step id / node / event / timestamp。验收：fake run 后生成可解析 JSONL，RunResult.trace_id 可关联到 trace 文件。
2. **事件覆盖** — 记录 plan、execute、reflect、confirm interrupt、takeover interrupt、end/error 的关键事件。验收：单测断言各事件类型在 fake graph run 中出现。
3. **脱敏与降级** — 图片、prompt、API key、隐私文本有明确脱敏/关闭策略；默认不把敏感截图上传第三方；LangFuse callback 未配置时不影响主流程。验收：未配置 LangFuse 时测试全绿；开启脱敏后 trace 不含原始敏感字段。
4. **Eval 关联** — Evaluation Harness 能把失败任务结果关联到 trace 文件与具体 step。验收：eval JSON 中包含 trace_id / trace_path 或等价字段。

**风险/边界**：
- LangFuse 是可选增强，不得成为运行、测试或本地评测的硬依赖。
- trace 不改变图路由语义；观测逻辑失败不得导致任务执行失败，除非显式开启 strict tracing。
- 不把复杂 dashboard 建设放入本 Phase；先保证可记录、可导出、可关联。

### Phase 9: 策略级 Reflection 与失败分类

**目标**：把当前二元 `continue/retry` 反思升级为可评测的失败原因分类与恢复策略建议，提升长任务鲁棒性。

**根因**：当前 `reflect_node` 主要通过 `raw_action.startswith("continue")` 判断成功，失败后缺少 cause 与 strategy；面对真实 app、长任务、多 app 时，盲目 retry 会增加步数并降低成功率。

**修改文件候选**：
- `phone_agent/graph/state.py`
- `phone_agent/graph/nodes/reflect.py`
- `phone_agent/graph/nodes/plan.py`
- `phone_agent/graph/edges.py`（仅当策略需要影响 replan/end，优先保持拓扑不变）
- `phone_agent/config/prompts_zh.py`
- `phone_agent/config/prompts_en.py`
- `tests/graph/test_reflection_strategy.py`
- `evals/` 评测任务与结果 schema

**执行步骤与验收标准**：
1. **Reflection schema** — State 能记录 `reflection_verdict`、`failure_cause`、`suggested_strategy` 等结构化字段。验收：解析成功、解析失败、安全降级均有单测覆盖。
2. **失败分类** — 至少支持 `succeeded` / `failed` / `partial` verdict，以及 `element_not_found`、`wrong_page`、`app_not_responding`、`network_or_loading`、`unsafe_or_sensitive` 等原因分类。验收：中英文 prompt 输出样例均能解析到结构化字段。
3. **Plan 反馈闭环** — Plan 节点能在下一轮看到上一轮结构化 reflection，用于调整动作而不是盲目重复。验收：fake model 单测断言下一轮 user message 或 state context 包含 failure cause/strategy。
4. **评测统计** — Evaluation Harness 能统计失败原因分布与 retry 次数，策略变更前后可比较平均步数/成功率。验收：eval JSON 汇总包含 failure_cause histogram 与 retry count。

**风险/边界**：
- 不改成模型自主动态路由；仍由条件边和 State 字段保持可审计控制。
- 不引入多 Agent critic/reflection agent；保持单图主线。
- Reflection schema 必须兼容中英文提示，不能破坏现有 `lang` 行为。

### Phase 10: Minimal Checkpoint/Resume — HITL 与长任务持久恢复

**目标**：在 UI grounding 前先落地最小可用 checkpoint/resume，使 confirm/takeover 中断、pending execute、普通 replan 路径可持久恢复、可测试、可评测。

**根因**：当前 Phase 2 只保证 LangGraph interrupt 路由语义，不保证跨进程或跨会话恢复；如果在没有 resume 契约前引入 UI grounding，会把定位失败、人工中断与状态恢复问题混在一起，难以验证。

**修改文件候选**：
- `phone_agent/agent.py`
- `phone_agent/graph/builder.py`
- `phone_agent/graph/state.py`
- `phone_agent/config/` 下新增或扩展 checkpoint 配置（执行阶段决定具体文件）
- `tests/graph/test_checkpoint_resume.py`
- `tests/evals/test_resume_metrics.py`
- `README.md`

**执行步骤与验收标准**：
1. **Checkpoint 编译入口** — 支持 LangGraph checkpointer 或等价机制保存 run/thread state，默认本地、可关闭。验收：不开启 checkpoint 时旧流程不变；开启后 graph compile/invoke 接受稳定 `thread_id`。
2. **Resume API** — 提供最小 `resume(thread_id, interrupt_result=...)` 或等价 API，能从 confirm/takeover interrupt 后继续。验收：fake confirm accepted 后恢复到 execute；confirm rejected 后结束或按既有语义处理。
3. **三类路径覆盖** — checkpoint/resume 测试覆盖 pending interrupt、pending execute、普通 replan 三类路径。验收：`.venv/bin/pytest tests/graph/test_checkpoint_resume.py -q` 全绿。
4. **Eval 指标扩展** — Evaluation Harness 在 checkpoint phase 后再统计 resume_success、resume_duration 等指标；Phase 7 仅统计 interrupt routing。验收：eval 文档和 JSON schema 明确区分 `hitl_count` 与 `resume_success`。

**风险/边界**：
- checkpoint/resume 不得绕过 HITL：敏感动作恢复后仍必须遵循 confirm-then-execute 约束。
- 默认不得长期保存敏感截图/API key/隐私文本；必须提供清理、关闭或脱敏策略。
- 不在本 Phase 做 Accessibility/OCR/UI grounding，避免并行引入两个高风险状态来源。

### Phase 11: Accessibility / OCR / UI Grounding

**目标**：在已有测试、structured eval、trace 与 checkpoint/resume 之后，提升 UI 定位可靠性，降低真实 app 动态 UI、遮挡、滚动与小控件带来的失败率。

**根因**：2026 手机 GUI Agent 趋势强调 UI grounding；仅靠截图 VLM + 相对坐标在小控件、列表、弹窗、动态页面中不稳定。但 grounding 应建立在可恢复、可 trace、可评测的基础上，否则难以判断收益。

**修改文件候选**：
- `phone_agent/adb/`（accessibility dump 或 OCR 所需设备能力，执行阶段验证现有 adb 抽象后决定）
- `phone_agent/device_factory.py`
- `phone_agent/graph/state.py`
- `phone_agent/graph/nodes/plan.py`
- `phone_agent/graph/tools/coords.py`
- `tests/graph/test_grounding.py`
- `tests/evals/test_grounding_eval.py`
- `README.md`

**执行步骤与验收标准**：
1. **Grounding context 注入** — 计划上下文可选注入 UI hierarchy / OCR elements / grounded candidates，模型仍输出 0-1000 相对坐标。验收：fake accessibility/OCR 数据能进入 plan context 与 trace。
2. **坐标约束保持** — tool 层继续统一调用 `convert_relative_to_absolute()` 转绝对坐标；grounding 结果不得绕过 tool 坐标转换。验收：坐标转换单测覆盖 grounded candidate 与普通 Tap 两类路径。
3. **降级路径** — 对无法获取 accessibility 或 OCR 的设备有降级路径，回到 screenshot-only，不影响基本运行。验收：accessibility/OCR 异常时 graph fake run 不失败，并记录降级事件。
4. **收益评测** — Evaluation Harness 能对 grounding on/off 做 A/B，对比成功率、平均步数、失败原因。验收：至少一个 fake 或本地 smoke 任务展示 grounding 指标字段，不以单次 demo 作为成功依据。

**风险/边界**：
- 不做 iOS 主线；Accessibility/OCR 只服务 Android 主线。
- 不破坏坐标转换约束：任何 grounding 结果最终仍必须经过 tool 层统一转换与执行。
- Grounding 数据进入 trace 时必须脱敏；不得默认上传或长期保存敏感 UI 文本。

## 后置 / 暂不做

- **跨会话 Memory 与技能轨迹复用**：必须等评测、trace、checkpoint 和足够重复成功轨迹后再做；届时需明确 session memory、cross-session memory、skill/procedural memory 的边界，且 memory 不得绕过 HITL、安全确认或当前设备状态检查。
- **Dreaming**：需要大量真实会话轨迹才有统计意义；个人/小型项目数据量不足，复杂度高、ROI 低。
- **多 Agent 架构**：手机自动化主线工具同质，当前不需要 Supervisor + Specialists；多 Agent 会增加路由和调试复杂度。
- **Command 动态路由**：当前条件边已覆盖路由，规则路由更可审计；不让模型在 Node 内动态决定拓扑。
- **复杂 Prompt 外部化**：Prompt 与 `do(action=...)` 格式强耦合；没有多租户/A-B 需求前不外部化为复杂 YAML/JSON 系统。
- **iOS 主线**：本路线聚焦 Android 真实 app 与 adb/accessibility/OCR；iOS 不作为当前主线目标。

# 约束 Checklist

修改 `phone_agent/graph/`、评测、trace、checkpoint、grounding、memory 或相关文档前，确认：

- [ ] **Design Loop**：`design_status=critic_approved`、`last_critic_verdict=APPROVE`、`approved_for_execution=true` 三者一致后才能实现；Phase 完成后先更新本文件，再按用户要求决定是否 commit。
- [ ] **Graph 不变量**：State schema 先行；不用 while loop 替代 StateGraph；不恢复 `_execute_step` 双轨；Node 不使用 `self._context`，上下文走 State `messages` + config。
- [ ] **依赖注入**：DeviceFactory / ModelClient 通过 StateGraph config 注入；tool 不直接 `get_device_factory()`；`device_factory` 不得进入 `@tool` 模型可见 schema，需有 fake-device 与 schema 断言测试。
- [ ] **动作安全**：坐标保持 0-1000 相对值，绝对像素转换只在 execute/tool 内调用 `convert_relative_to_absolute()`；动作解析用 `ast.parse` + `ast.literal_eval`，禁止 `eval()`。
- [ ] **上下文与 reducer**：每步后剥离历史图片；`plan_node` 只返回新增 messages，`execute_node` 返回完整重建列表；role/content 边界必须有测试。
- [ ] **HITL / confirm-then-execute**：敏感操作走 `confirm_node`，登录/验证码走 `takeover_node`，均用 `interrupt()`；当前只承诺 interrupt 路由语义，持久 resume 等 Phase 10 完成后再对外承诺。
- [ ] **pending_execute 约束**：`after_interrupt` 仅在 `pending_execute=True & interrupt_result=True` 时到 `execute`；pending 分支不得 `_strip_and_append`，必须设置 `action_confirmed=True` 并保留 `action_parsed`；`after_execute` 先检查 `action_confirmed`；`plan_node` 每轮重置 `action_confirmed=False`；`confirm_node` 显式返回 `pending_execute`。
- [ ] **Phase 6 门禁**：保留 `setup.py` 并补 runtime deps；`.gitignore` 不得忽略 `tests/`；测试/文档不得声明未经验证的 passing 数；README 拓扑必须与 `builder.py` 一致。
- [ ] **Eval / Trace**：`PhoneAgent.run() -> str` 保持兼容；结构化评测使用 `run_structured()` 或等价参数；Evaluation Harness 至少记录 success/steps/duration/error/HITL count；Trace 默认本地可用，LangFuse 可选且未配置不影响主流程。
- [ ] **隐私与收益验证**：Trace/checkpoint/memory 默认不得上传或长期保存敏感截图/API key/隐私文本；Reflection、grounding、memory 的收益必须通过 Evaluation Harness 对比验证。
- [ ] **后续能力边界**：checkpoint/resume 先于 UI grounding，且不得绕过 HITL；Accessibility/OCR 不破坏 Android 主线和坐标转换约束；Memory 不得绕过 HITL/安全确认/当前设备状态检查。
- [ ] **暂不做**：Dreaming、多 Agent 架构、Command 动态路由、复杂 Prompt 外部化、iOS 主线、早期技能记忆。
