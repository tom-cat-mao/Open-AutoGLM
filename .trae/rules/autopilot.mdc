---
description: Autopilot 使用 OMX canonical workflow；旧 Trae-native 状态机已移除
alwaysApply: false
globs: ".codex/skills/autopilot/SKILL.md,.codex/skills/deep-interview/SKILL.md,.codex/skills/ralplan/SKILL.md,.codex/skills/ultragoal/SKILL.md,.codex/skills/code-review/SKILL.md,.codex/skills/ultraqa/SKILL.md,.trae/traecli.toml,.trae/rules/graph.mdc"
---

# Autopilot Boundary

Open-AutoGLM 不再维护 Trae-native Autopilot 状态机。

## Canonical workflow

Autopilot 必须使用 oh-my-codex 最新 workflow：

```text
$deep-interview -> $ralplan -> $ultragoal (+ $team if needed) -> $code-review -> $ultraqa
```

详细协议以 `.codex/skills/autopilot/SKILL.md` 为准。

## Removed legacy surfaces

以下旧入口已删除，不得恢复：

- `.trae/skills/autopilot/`
- `.trae/commands/autopilot.md`
- `.trae/hooks/autopilot.py`
- `.trae/autopilot/state.json` / `.trae/autopilot/transitions.jsonl` 作为 workflow runtime

## State and artifact boundary

- Workflow state 使用 OMX：`.omx/state/**` 与 `omx state ... --json`。
- Planning/context artifacts 使用 OMX：`.omx/context/**`、`.omx/plans/**`、`.omx/specs/**`。
- Durable execution 使用 OMX Ultragoal：`.omx/ultragoal/**` 与 Codex/TraeCLI goal surface。
- `.trae/rules/graph.mdc` 只保留 Open-AutoGLM 架构 roadmap、PhoneAgent/LangGraph/Grounding 不变量和项目约束镜像；不再作为 Autopilot runtime state。

## Project constraints

Autopilot 执行 Open-AutoGLM 任务时仍必须遵守 AGENTS.md 与 `.trae/rules/graph.mdc` 中的 PhoneAgent、LangGraph、ActionIR、HITL、Grounding、privacy、benchmark 约束。
